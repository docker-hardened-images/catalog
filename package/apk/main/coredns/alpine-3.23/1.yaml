# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-coredns
variant: runtime
tags:
  - 1.14.1-alpine3.23
  - 1.14.1-r0-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 80527fd389841310a728cf862491129321c8a112
  PACKAGE_NAME: coredns
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "1.14"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.14.1
  VERSION: 1.14.1
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: coredns
      work-dir: /go/src/github.com/coredns/coredns
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
          - libcap-utils
        files:
          - url: git+https://github.com/coredns/coredns.git#v1.14.1
            checksum: 80527fd389841310a728cf862491129321c8a112
            path: /go/src/github.com/coredns/coredns
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps: []
            tidy: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/coredns/coredns/coremain.CoreVersion=1.14.1
              - -X github.com/coredns/coredns/coremain.GitCommit=80527fd389841310a728cf862491129321c8a112
            output: ${target.dir}/usr/bin/coredns
            packages: .
        - name: build dev
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/coredns/coredns/coremain.CoreVersion=1.14.1
              - -X github.com/coredns/coredns/coremain.GitCommit=80527fd389841310a728cf862491129321c8a112
            output: ${target.dir}/usr/bin/coredns-dev
            packages: .
            strip: false
        - name: set capabilities
          runs: |
            set -eux -o pipefail

            setcap cap_net_bind_service=p "${target.dir}/usr/bin/coredns"
        - uses: sbom@v1
          with:
            prefix: coredns-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/coredns
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/coredns
          with:
            description: DNS and Service Discovery
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/coredns-dev" "$subpkgdir/usr/bin/coredns"
            license: Apache-2.0
            name: coredns
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/coredns/coredns
            version: 1.14.1
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/coredns
        - uses: abuild/upload@v1
    - name: coredns-fips
      work-dir: /go/src/github.com/coredns/coredns
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
          - libcap-utils
        files:
          - url: git+https://github.com/coredns/coredns.git#v1.14.1
            checksum: 80527fd389841310a728cf862491129321c8a112
            path: /go/src/github.com/coredns/coredns
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps: []
            tidy: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: true
            ldflags:
              - -X github.com/coredns/coredns/coremain.CoreVersion=1.14.1
              - -X github.com/coredns/coredns/coremain.GitCommit=80527fd389841310a728cf862491129321c8a112
            output: ${target.dir}/usr/bin/coredns
            packages: .
        - name: build dev
          uses: go/build@v1
          with:
            fips: true
            ldflags:
              - -X github.com/coredns/coredns/coremain.CoreVersion=1.14.1
              - -X github.com/coredns/coredns/coremain.GitCommit=80527fd389841310a728cf862491129321c8a112
            output: ${target.dir}/usr/bin/coredns-dev
            packages: .
            strip: false
        - name: set capabilities
          runs: |
            set -eux -o pipefail

            setcap cap_net_bind_service=p "${target.dir}/usr/bin/coredns"
        - uses: sbom@v1
          with:
            prefix: coredns-fips-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/coredns-fips
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/coredns-fips
          with:
            description: DNS and Service Discovery
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/coredns-dev" "$subpkgdir/usr/bin/coredns"
            license: Apache-2.0
            name: coredns-fips
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/coredns/coredns
            version: 1.14.1
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/coredns-fips
        - uses: abuild/upload@v1
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
