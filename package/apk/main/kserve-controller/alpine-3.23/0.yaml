# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-kserve-controller
variant: runtime
tags:
  - 0.16.0-alpine3.23
  - 0.16.0-r0-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 5b033a4024429302440b72180472ae2d26b44086
  PACKAGE_NAME: kserve-controller
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "0.16"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.16.0
  VERSION: 0.16.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: kserve-controller
      work-dir: /go/src/github.com/kserve/kserve
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
          - ca-certificates
          - clang
          - cmake
          - openssl
        files:
          - url: git+https://github.com/kserve/kserve.git#v0.16.0
            checksum: 5b033a4024429302440b72180472ae2d26b44086
            path: /go/src/github.com/kserve/kserve
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps:
              - golang.org/x/crypto@v0.45.0
              - github.com/expr-lang/expr@v1.17.7
              - github.com/kedacore/keda/v2@v2.17.3
              - go.opentelemetry.io/otel/sdk@v1.40.0
            download: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: false
            output: ${target.dir}/usr/bin/manager
            packages: ./cmd/manager
        - name: build dev
          uses: go/build@v1
          with:
            fips: false
            output: ${target.dir}/usr/bin/manager-dev
            packages: ./cmd/manager
            strip: false
        - uses: sbom@v1
          with:
            prefix: kserve-controller-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/kserve-controller
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/kserve-controller
          with:
            description: KServe controller for managing inference services
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/manager-dev" "$subpkgdir/usr/bin/manager"
            license: Apache-2.0
            name: kserve-controller
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/kserve/kserve
            version: 0.16.0
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/kserve-controller
    - name: kserve-controller-fips
      work-dir: /go/src/github.com/kserve/kserve
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
          - ca-certificates
          - clang
          - cmake
          - openssl
        files:
          - url: git+https://github.com/kserve/kserve.git#v0.16.0
            checksum: 5b033a4024429302440b72180472ae2d26b44086
            path: /go/src/github.com/kserve/kserve
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps:
              - golang.org/x/crypto@v0.45.0
              - github.com/expr-lang/expr@v1.17.7
              - github.com/kedacore/keda/v2@v2.17.3
              - go.opentelemetry.io/otel/sdk@v1.40.0
            download: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: true
            output: ${target.dir}/usr/bin/manager
            packages: ./cmd/manager
        - name: build dev
          uses: go/build@v1
          with:
            fips: true
            output: ${target.dir}/usr/bin/manager-dev
            packages: ./cmd/manager
            strip: false
        - uses: sbom@v1
          with:
            prefix: kserve-controller-fips-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/kserve-controller-fips
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/kserve-controller-fips
          with:
            description: KServe controller for managing inference services
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/manager-dev" "$subpkgdir/usr/bin/manager"
            license: Apache-2.0
            name: kserve-controller-fips
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/kserve/kserve
            version: 0.16.0
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/kserve-controller-fips
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
