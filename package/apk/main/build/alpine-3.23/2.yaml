# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-build
variant: runtime
tags:
  - 2.6.4-alpine3.23
  - 2.6.4-r1-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 0905d03f24161219c5a93ad09e696b49c9fe1ad8
  PACKAGE_NAME: build
  REL: "1"
  SEMVER_MAJOR_MINOR_VERSION: "2.6"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.6.4
  VERSION: 2.6.4
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: build
      work-dir: /go/src/github.com/docker-hardened-images/frontend
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
        files:
          - url: git+https://github.com/docker-hardened-images/frontend.git#v2.6.4
            checksum: 0905d03f24161219c5a93ad09e696b49c9fe1ad8
            path: /go/src/github.com/docker-hardened-images/frontend
            uid: 0
            gid: 0
      pipeline:
        - privileged: true
          runs: |
            set -eux -o pipefail

            go get go.opentelemetry.io/otel/sdk@v1.40.0 # CVE-2026-24051
            go mod tidy
            go mod vendor
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: false
            output: ${target.dir}/usr/bin/build
            packages: ./cmd/dhi-build
        - name: build dev
          uses: go/build@v1
          with:
            fips: false
            output: ${target.dir}/usr/bin/build-dev
            packages: ./cmd/dhi-build
            strip: false
        - uses: sbom@v1
          with:
            prefix: build-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/build
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/build
          with:
            description: Buildkit frontend for building Docker Hardened Images
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/build-dev" "$subpkgdir/usr/bin/build"
            license: Apache-2.0
            name: build
            options: '!check !strip'
            rel: 1
            subpackages: $pkgname-dev
            url: https://github.com/docker-hardened-images/frontend
            version: 2.6.4
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/build
        - uses: abuild/upload@v1
    - name: build-fips
      work-dir: /go/src/github.com/docker-hardened-images/frontend
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
        files:
          - url: git+https://github.com/docker-hardened-images/frontend.git#v2.6.4
            checksum: 0905d03f24161219c5a93ad09e696b49c9fe1ad8
            path: /go/src/github.com/docker-hardened-images/frontend
            uid: 0
            gid: 0
      pipeline:
        - privileged: true
          runs: |
            set -eux -o pipefail

            go get go.opentelemetry.io/otel/sdk@v1.40.0 # CVE-2026-24051
            go mod tidy
            go mod vendor
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: true
            output: ${target.dir}/usr/bin/build
            packages: ./cmd/dhi-build
        - name: build dev
          uses: go/build@v1
          with:
            fips: true
            output: ${target.dir}/usr/bin/build-dev
            packages: ./cmd/dhi-build
            strip: false
        - uses: sbom@v1
          with:
            prefix: build-fips-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/build-fips
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/build-fips
          with:
            description: Buildkit frontend for building Docker Hardened Images
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/build-dev" "$subpkgdir/usr/bin/build"
            license: Apache-2.0
            name: build-fips
            options: '!check !strip'
            rel: 1
            subpackages: $pkgname-dev
            url: https://github.com/docker-hardened-images/frontend
            version: 2.6.4
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/build-fips
        - uses: abuild/upload@v1
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
