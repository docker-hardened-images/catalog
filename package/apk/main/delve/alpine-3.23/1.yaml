# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-delve
variant: runtime
tags:
  - 1.26.0-alpine3.23
  - 1.26.0-r0-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 4e4f29ce753a0dda04b200452c23b90a25923465
  PACKAGE_NAME: delve
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "1.26"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.26.0
  VERSION: 1.26.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: delve
      work-dir: /go/src/github.com/go-delve/delve
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
        files:
          - url: git+https://github.com/go-delve/delve.git#v1.26.0
            checksum: 4e4f29ce753a0dda04b200452c23b90a25923465
            path: /go/src/github.com/go-delve/delve
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps: []
            tidy: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/go-delve/delve/main.Version=1.26.0
              - -X github.com/go-delve/delve/main.Build=4e4f29ce753a0dda04b200452c23b90a25923465
            output: ${target.dir}/usr/bin/dlv
            packages: cmd/dlv/main.go
        - name: build dev
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/go-delve/delve/main.Version=1.26.0
              - -X github.com/go-delve/delve/main.Build=4e4f29ce753a0dda04b200452c23b90a25923465
            output: ${target.dir}/usr/bin/dlv-dev
            packages: cmd/dlv/main.go
            strip: false
        - uses: sbom@v1
          with:
            prefix: delve-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/delve
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/delve
          with:
            description: A debugger for the Go programming language
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/dlv-dev" "$subpkgdir/usr/bin/dlv"
            license: MIT
            name: delve
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/go-delve/delve
            version: 1.26.0
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/delve
    - name: delve-fips
      work-dir: /go/src/github.com/go-delve/delve
      contents:
        packages:
          - busybox
          - bash
          - golang-1.25
          - golang-1.25-oci-config
        files:
          - url: git+https://github.com/go-delve/delve.git#v1.26.0
            checksum: 4e4f29ce753a0dda04b200452c23b90a25923465
            path: /go/src/github.com/go-delve/delve
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps: []
            tidy: true
        - uses: go/hermetic@v1
        - name: build
          uses: go/build@v1
          with:
            fips: true
            ldflags:
              - -X github.com/go-delve/delve/main.Version=1.26.0
              - -X github.com/go-delve/delve/main.Build=4e4f29ce753a0dda04b200452c23b90a25923465
            output: ${target.dir}/usr/bin/dlv
            packages: cmd/dlv/main.go
        - name: build dev
          uses: go/build@v1
          with:
            fips: true
            ldflags:
              - -X github.com/go-delve/delve/main.Version=1.26.0
              - -X github.com/go-delve/delve/main.Build=4e4f29ce753a0dda04b200452c23b90a25923465
            output: ${target.dir}/usr/bin/dlv-dev
            packages: cmd/dlv/main.go
            strip: false
        - uses: sbom@v1
          with:
            prefix: delve-fips-go
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/delve-fips
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/delve-fips
          with:
            description: A debugger for the Go programming language
            functions:
              dev: |
                pkgdesc="$pkgdesc (dev)"
                mkdir -p "$subpkgdir/usr/bin"
                mv "$pkgdir/usr/bin/dlv-dev" "$subpkgdir/usr/bin/dlv"
            license: MIT
            name: delve-fips
            options: '!check !strip'
            rel: 0
            subpackages: $pkgname-dev
            url: https://github.com/go-delve/delve
            version: 1.26.0
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/delve-fips
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
