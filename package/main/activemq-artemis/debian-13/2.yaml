# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-activemq-artemis
variant: runtime
tags:
  - 2.55.0
  - 2.55.0-r0
  - 2.55.0-debian13
  - 2.55.0-r0-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  ARCH: '#{ target.arch == "amd64" ? "x64" : "aarch64" }'
  COMMIT_SHA: b2f1018553017a411d55bb6e09cefc29d49eb4c2
  JDK_MAJOR_VERSION: "21"
  JDK_VERSION: 21.0.12.8-0
  MAVEN_VERSION: 3.9.16
  PACKAGE_NAME: activemq-artemis
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "2.55"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.55.0
  VERSION: 2.55.0
contents:
  packages:
    - base-files
  builds:
    - name: activemq-artemis
      work-dir: ${source.dir}/activemq-artemis
      environment:
        JAVA_HOME: /usr/lib/jvm/temurin-21
        MAVEN_HOME: /opt/java/apache-maven-3.9.16
      caches:
        - path: /root/.m2/repository
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - bash
          - ca-certificates
          - coreutils
          - findutils
          - git
          - gzip
          - sed
          - tar
          - eclipse-temurin-21-jdk=21.0.12.8-0
        files:
          - url: git+https://github.com/apache/activemq-artemis.git#2.55.0
            checksum: b2f1018553017a411d55bb6e09cefc29d49eb4c2
            path: ${source.dir}/activemq-artemis
            spdx:
              name: activemq-artemis
              version: 2.55.0
              packages:
                - name: activemq-artemis
                  purl: pkg:dhi/activemq-artemis@2.55.0
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.tar.gz
            path: ${source.dir}/maven/apache-maven-3.9.16-bin.tar.gz
            spdx:
              name: maven
              version: 3.9.16
              packages:
                - name: maven
                  purl: pkg:generic/apache-maven@3.9.16
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: https://repo1.maven.org/maven2/io/netty/netty-transport-native-epoll/4.1.136.Final/netty-transport-native-epoll-4.1.136.Final-linux-x86_64.jar
            checksum: sha256:c3956f90241582bbbad5612c3159cffeb7c3533324759f3c3bd778ca9b176cae
            path: ${source.dir}/patch/deps/netty-transport-native-epoll-4.1.136.Final-linux-x86_64.jar
            uid: 0
            gid: 0
          - url: https://repo1.maven.org/maven2/io/netty/netty-transport-native-epoll/4.1.136.Final/netty-transport-native-epoll-4.1.136.Final-linux-aarch_64.jar
            checksum: sha256:f264cf4bcc1e97ad148cd4f0d8777eacf404cd4ed80830de3f8a2359f60edc38
            path: ${source.dir}/patch/deps/netty-transport-native-epoll-4.1.136.Final-linux-aarch_64.jar
            uid: 0
            gid: 0
          - url: https://repo1.maven.org/maven2/io/netty/netty-transport-native-kqueue/4.1.136.Final/netty-transport-native-kqueue-4.1.136.Final-osx-x86_64.jar
            checksum: sha256:22aef11d1927cf80078c120936885c37fd1f7ad2ccfcafba10903ed241563f2d
            path: ${source.dir}/patch/deps/netty-transport-native-kqueue-4.1.136.Final-osx-x86_64.jar
            uid: 0
            gid: 0
          - url: https://repo1.maven.org/maven2/io/netty/netty-transport-native-kqueue/4.1.136.Final/netty-transport-native-kqueue-4.1.136.Final-osx-aarch_64.jar
            checksum: sha256:fff9807e64174e25b716929330937f68bde3217e5ee80d8b8e2e13277cc9d6e4
            path: ${source.dir}/patch/deps/netty-transport-native-kqueue-4.1.136.Final-osx-aarch_64.jar
            uid: 0
            gid: 0
      pipeline:
        - name: install maven
          runs: |
            set -eux -o pipefail

            mkdir -p /opt/java
            tar -xzf "${source.dir}/maven/apache-maven-3.9.16-bin.tar.gz" -C /opt/java

            mkdir -p /usr/local/bin
            find "/opt/java/apache-maven-3.9.16/bin" -type f -executable -o -type l | while read -r file; do
              filename=$(basename "$file")
              ln -sf "/opt/java/apache-maven-3.9.16/bin/$filename" "/usr/local/bin/$filename"
            done
        - name: configure maven mirror
          runs: |
            set -eux -o pipefail

            mkdir -p /root/.m2
            cat > /root/.m2/settings.xml << 'EOF'
            <?xml version="1.0" encoding="UTF-8"?>
            <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
              <mirrors>
                <mirror>
                  <id>google-maven-central</id>
                  <name>GCS Maven Central mirror</name>
                  <url>https://maven-central.storage-download.googleapis.com/maven2/</url>
                  <mirrorOf>central</mirrorOf>
                </mirror>
              </mirrors>
            </settings>
            EOF
        - name: patch dependencies
          uses: mvn/patch@v1
          with:
            dependencies: ${source.dir}/patch/mvn-dependencies.json
            mvn-root: ${source.dir}/activemq-artemis
            properties: ${source.dir}/patch/mvn-properties.json
        - name: build
          privileged: true
          runs: |
            set -eux -o pipefail

            if [ "${ARCH}" = "aarch64" ]; then
              export MAVEN_OPTS="-Dos.arch=aarch64 -Dos.name=Linux -Dos.version=arm64"
              export JAVA_OPTS="-Dos.arch=aarch64"
            fi

            mvn clean install -DskipTests -Dmaven.javadoc.skip=true
        - name: patch native netty classifier jars
          runs: |
            set -eux -o pipefail

            dist_lib="${source.dir}/activemq-artemis/artemis-distribution/target/apache-artemis-2.55.0-bin/apache-artemis-2.55.0/lib"
            netty_version="4.1.136.Final"

            for arch in x86_64 aarch_64; do
              find "${dist_lib}" \
                -name "netty-transport-native-epoll-*-linux-${arch}.jar" \
                ! -name "netty-transport-native-epoll-${netty_version}-linux-${arch}.jar" \
                -delete
              install -m 0644 \
                "${source.dir}/patch/deps/netty-transport-native-epoll-${netty_version}-linux-${arch}.jar" \
                "${dist_lib}/netty-transport-native-epoll-${netty_version}-linux-${arch}.jar"
            done

            for arch in x86_64 aarch_64; do
              stale_kqueue=$(find "${dist_lib}" \
                -name "netty-transport-native-kqueue-*-osx-${arch}.jar" \
                ! -name "netty-transport-native-kqueue-${netty_version}-osx-${arch}.jar" \
                -print)
              if [ -z "${stale_kqueue}" ]; then
                continue
              fi
              find "${dist_lib}" \
                -name "netty-transport-native-kqueue-*-osx-${arch}.jar" \
                ! -name "netty-transport-native-kqueue-${netty_version}-osx-${arch}.jar" \
                -delete
              install -m 0644 \
                "${source.dir}/patch/deps/netty-transport-native-kqueue-${netty_version}-osx-${arch}.jar" \
                "${dist_lib}/netty-transport-native-kqueue-${netty_version}-osx-${arch}.jar"
            done
        - name: stage distribution
          runs: |
            set -eux -o pipefail

            dist="${source.dir}/activemq-artemis/artemis-distribution/target/apache-artemis-2.55.0-bin/apache-artemis-2.55.0"
            install -d "${target.dir}/usr/share/activemq-artemis"
            cp -a "${dist}/." "${target.dir}/usr/share/activemq-artemis/"
            install -D -m 0755 "${source.dir}/entrypoint.sh" \
              "${target.dir}/usr/share/activemq-artemis/bin/entrypoint.sh"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - bash
              - coreutils
            description: Apache ActiveMQ Artemis high-performance messaging broker
            files:
              - usr
            formats:
              - deb
            license: Apache-2.0
            name: activemq-artemis
            rel: 0
            url: https://activemq.apache.org/components/artemis/
            version: 2.55.0
      paths:
        - type: file
          path: ${source.dir}/entrypoint.sh
          uid: 0
          gid: 0
          source: package/main/activemq-artemis/bin/entrypoint.sh
        - type: file
          path: ${source.dir}/patch/mvn-dependencies.json
          uid: 0
          gid: 0
          source: package/main/activemq-artemis/patch/mvn-dependencies.json
        - type: file
          path: ${source.dir}/patch/mvn-properties.json
          uid: 0
          gid: 0
          source: package/main/activemq-artemis/patch/mvn-properties.json
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
