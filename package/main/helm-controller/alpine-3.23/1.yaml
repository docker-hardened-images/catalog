# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-helm-controller
variant: runtime
tags:
  - 1.6.3-alpine3.23
  - 1.6.3-r0-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-06-17"
vars:
  COMMIT_SHA: 372c34e2b4a455d24ef6cbb8321c82c4533dbbd8
  GOLANG_MAJOR_MINOR_VERSION: "1.26"
  GOLANG_VERSION: 1.26.5-r0
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "1.6"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.6.3
  VERSION: 1.6.3
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: helm-controller
      work-dir: /go/src/github.com/fluxcd/helm-controller
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - coreutils
          - findutils
          - mawk
          - sed
          - golang-1.26=1.26.5-r0
          - golang-1.26-oci-config=1.26.5-r0
        files:
          - url: git+https://github.com/fluxcd/helm-controller#v1.6.3
            checksum: 372c34e2b4a455d24ef6cbb8321c82c4533dbbd8
            path: /go/src/github.com/fluxcd/helm-controller
            spdx:
              name: fluxcd-helm-controller
              version: 1.6.3
              packages:
                - name: fluxcd-helm-controller
                  purl: pkg:golang/github.com/fluxcd/helm-controller@1.6.3
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: patch (1 only)
          uses: go/bump@v2
          with:
            deps: []
            download: true
        - name: test
          runs: |
            set -eux -o pipefail

            # Exclude packages that require envtest (live k8s API server via etcd):
            # internal/controller, internal/controller_test, internal/action, internal/reconcile
            CGO_ENABLED=0 go test $(go list ./internal/... | grep -vE '/internal/(controller|action|reconcile)')
        - name: build
          uses: go/build@v1
          environment:
            GOFLAGS: -buildvcs=false
          with:
            fips: false
            output: ${target.dir}/usr/bin/fluxcd-helm-controller
        - name: build dev
          uses: go/build@v1
          work-dir: /go/src/github.com/fluxcd/helm-controller
          with:
            fips: false
            output: ${target.dir}/usr/bin/fluxcd-helm-controller-dev
            packages: .
            strip: false
        - uses: dhi/build-pkg:0
          with:
            description: Helm-controller
            files:
              - usr/bin/fluxcd-helm-controller
            license: Apache-2.0
            name: fluxcd-helm-controller
            rel: 0
            subpackages:
              - files:
                  - path: usr/bin/fluxcd-helm-controller
                    source: usr/bin/fluxcd-helm-controller-dev
                name: fluxcd-helm-controller-dev
            url: https://github.com/fluxcd/helm-controller
            version: 1.6.3
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
