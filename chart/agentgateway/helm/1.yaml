# syntax=dhi.io/build:2-helm3

name: Agentgateway Helm chart 1.x
image: dhi.io/agentgateway-chart
variant: helm
tags:
  - 1.2.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 6a2d02dbb35a289738d0fdba88e5a474e432a72a
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  AGENTGATEWAY_CONTROLLER_IMAGE_REFERENCE: dhi/agentgateway-controller:1.2.1-debian13
  AGENTGATEWAY_IMAGE_REFERENCE: dhi/agentgateway:1.2.1-debian13
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:462393863790e8d4b174f63f0da50798da095a059d4fab7692ae4fb7a58e99d3
  SEMVER_MAJOR_MINOR_VERSION: "1.2"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.2.1
  VERSION: 1.2.1
contents:
  builds:
    - name: agentgateway-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:462393863790e8d4b174f63f0da50798da095a059d4fab7692ae4fb7a58e99d3
      contents:
        files:
          - url: git+https://github.com/agentgateway/agentgateway.git#v1.2.1
            checksum: 6a2d02dbb35a289738d0fdba88e5a474e432a72a
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/controller/install/helm/agentgateway
          with:
            chart:
              appRef: agentgateway-controller
              description: Docker Hardened Images Helm chart for Agentgateway
              images:
                - name: agentgateway-controller
                  ref: dhi/agentgateway-controller:1.2.1-debian13
                - name: agentgateway
                  ref: dhi/agentgateway:1.2.1-debian13
              name: agentgateway-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/agentgateway/agentgateway
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/controller/install/helm/agentgateway
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/controller/install/helm
          with:
            chart: agentgateway
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/controller/install/helm
          with:
            chart: agentgateway
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/agentgateway/config
