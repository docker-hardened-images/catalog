# syntax=dhi.io/build:2-helm3

name: CloudNativePG Helm chart 0.x
image: dhi.io/cloudnative-pg-chart
variant: helm
tags:
  - 0.28.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 011ce31fbd7a6b92f239393864a0d2d41e9d38cb
  CLOUDNATIVE_PG_REFERENCE: dhi/cloudnative-pg:1.29.0-debian13@sha256:320d9667d93702ba90dae1716f82f06a0d10a621cd80399dfb9e95f1a9de729a
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:b72e12042810e0697eebb51a7862e7699f4df8eca02324029c220ccfd0bce4a4
  SEMVER_MAJOR_MINOR_VERSION: "0.28"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.28.0
  VERSION: 0.28.0
contents:
  builds:
    - name: cloudnative-pg
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:b72e12042810e0697eebb51a7862e7699f4df8eca02324029c220ccfd0bce4a4
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/cloudnative-pg/charts.git#cloudnative-pg-v0.28.0
            checksum: 011ce31fbd7a6b92f239393864a0d2d41e9d38cb
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/cloudnative-pg
          with:
            chart:
              appRef: cloudnative-pg
              description: Docker Hardened Images Helm chart for CloudNativePG
              images:
                - name: cloudnative-pg
                  ref: dhi/cloudnative-pg:1.29.0-debian13@sha256:320d9667d93702ba90dae1716f82f06a0d10a621cd80399dfb9e95f1a9de729a
              name: cloudnative-pg-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/cloudnative-pg/charts
              version: 0.28.0
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/cloudnative-pg
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: helm login
          uses: helm/login@v1
          with:
            registry: dhi.io
        - name: pull dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/charts/cloudnative-pg
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: cloudnative-pg
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: cloudnative-pg
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/cloudnative-pg/config
