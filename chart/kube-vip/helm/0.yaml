# syntax=dhi.io/build:2-helm3

name: kube-vip Helm chart 0.x
image: dhi.io/kube-vip-chart
variant: helm
tags:
  - 0.9.9
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 7a48df96cd5a1e60b6f5fd052379014a5f7d27f0
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
  KUBE_VIP_REFERENCE: dhi/kube-vip:1.2.0-debian13@sha256:a311886700205605120859d016170af6f366ed0b81863efe85f65f31b694db2a
  SEMVER_MAJOR_MINOR_VERSION: "0.9"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.9.9
  VERSION: 0.9.9
contents:
  builds:
    - name: kube-vip
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
      contents:
        files:
          - url: git+https://github.com/kube-vip/helm-charts.git#kube-vip-0.9.9
            checksum: 7a48df96cd5a1e60b6f5fd052379014a5f7d27f0
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/kube-vip
          with:
            chart:
              appRef: kube-vip
              description: Docker Hardened Images Helm chart for kube-vip
              images:
                - name: kube-vip
                  ref: dhi/kube-vip:1.2.0-debian13@sha256:a311886700205605120859d016170af6f366ed0b81863efe85f65f31b694db2a
              name: kube-vip-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kube-vip/helm-charts
            cmd: prepare
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/kube-vip
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: kube-vip
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/kube-vip
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kube-vip/config
