# syntax=dhi.io/build:2-helm3

name: kube-vip Helm chart 0.x
image: dhi.io/kube-vip-chart
variant: helm
tags:
  - 0.9.6
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: ba12cc6a5e48ebd80dddf2c78c867445dda34418
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.0-debian13-dev@sha256:6261312ab3d62801e9998b99b320c623846f10685a4cc085ef39aed161a2a42e
  KUBE_VIP_REFERENCE: dhi/kube-vip:1.0.4-debian13@sha256:2e2f49e0164f869fd70a802c7f175069de30d16eec9109f0f80a32396b65c998
  SEMVER_MAJOR_MINOR_VERSION: "0.9"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.9.6
  VERSION: 0.9.6
contents:
  builds:
    - name: kube-vip
      uses: dhi.io/helm:3.20.0-debian13-dev@sha256:6261312ab3d62801e9998b99b320c623846f10685a4cc085ef39aed161a2a42e
      contents:
        files:
          - url: git+https://github.com/kube-vip/helm-charts.git#kube-vip-0.9.6
            checksum: ba12cc6a5e48ebd80dddf2c78c867445dda34418
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/kube-vip
          with:
            chart:
              appRef: kube-vip
              description: Docker Hardened Images Helm chart for kube-vip
              images:
                - name: kube-vip
                  ref: dhi/kube-vip:1.0.4-debian13@sha256:2e2f49e0164f869fd70a802c7f175069de30d16eec9109f0f80a32396b65c998
              name: kube-vip-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kube-vip/helm-charts
            cmd: prepare
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/kube-vip
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: kube-vip
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/kube-vip
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kube-vip/config
