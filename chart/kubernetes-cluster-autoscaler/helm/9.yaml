# syntax=dhi.io/build:2-helm3

name: Kubernetes Cluster Autoscaler Helm chart 9.x
image: dhi.io/kubernetes-cluster-autoscaler-chart
variant: helm
tags:
  - 9.56.0
platforms:
  - linux/amd64
vars:
  AUTOSCALER_REFERENCE: dhi/kubernetes-cluster-autoscaler:1.34.3-debian13@sha256:f371aa2aae82033c2b9fb3d864f1f27badef023d1f1aab5f2098412cadaf0d0d
  CHART_COMMIT_SHA: 46bb45f204cef0114d9eca40294df467940ec55b
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.0-debian13-dev@sha256:d9fb15368eec534b9256a049385b9f843bca3f64736f14e4b6d593efdccfeaa9
  SEMVER_MAJOR_MINOR_VERSION: "9.56"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.56.0
  VERSION: 9.56.0
contents:
  builds:
    - name: kubernetes-cluster-autoscaler
      uses: dhi.io/helm:3.20.0-debian13-dev@sha256:d9fb15368eec534b9256a049385b9f843bca3f64736f14e4b6d593efdccfeaa9
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/kubernetes/autoscaler.git#cluster-autoscaler-chart-9.56.0
            checksum: 46bb45f204cef0114d9eca40294df467940ec55b
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            chart:
              appRef: kubernetes-cluster-autoscaler
              description: Docker Hardened Images Helm chart for kubernetes-cluster-autoscaler
              images:
                - name: kubernetes-cluster-autoscaler
                  ref: dhi/kubernetes-cluster-autoscaler:1.34.3-debian13@sha256:f371aa2aae82033c2b9fb3d864f1f27badef023d1f1aab5f2098412cadaf0d0d
              name: kubernetes-cluster-autoscaler-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubernetes/autoscaler
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kubernetes-cluster-autoscaler/config
