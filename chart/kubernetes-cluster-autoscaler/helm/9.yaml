# syntax=dhi.io/build:2-helm3

name: Kubernetes Cluster Autoscaler Helm chart 9.x
image: dhi.io/kubernetes-cluster-autoscaler-chart
variant: helm
tags:
  - 9.57.0
platforms:
  - linux/amd64
vars:
  AUTOSCALER_REFERENCE: dhi/kubernetes-cluster-autoscaler:1.34.3-debian13@sha256:ba41fc023d6e46d728dceb18c9537c3972f892a7637a2f551efe17bd05eb86f4
  CHART_COMMIT_SHA: 91080c84cfdfdd29c24e767d26977311c5a17ab1
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
  SEMVER_MAJOR_MINOR_VERSION: "9.57"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.57.0
  VERSION: 9.57.0
contents:
  builds:
    - name: kubernetes-cluster-autoscaler
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/kubernetes/autoscaler.git#cluster-autoscaler-chart-9.57.0
            checksum: 91080c84cfdfdd29c24e767d26977311c5a17ab1
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            chart:
              appRef: kubernetes-cluster-autoscaler
              description: Docker Hardened Images Helm chart for kubernetes-cluster-autoscaler
              images:
                - name: kubernetes-cluster-autoscaler
                  ref: dhi/kubernetes-cluster-autoscaler:1.34.3-debian13@sha256:ba41fc023d6e46d728dceb18c9537c3972f892a7637a2f551efe17bd05eb86f4
              name: kubernetes-cluster-autoscaler-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubernetes/autoscaler
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kubernetes-cluster-autoscaler/config
