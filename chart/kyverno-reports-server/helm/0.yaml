# syntax=dhi.io/build:2-helm3

name: Kyverno Reports Server Helm chart 0.x
image: dhi.io/kyverno-reports-server-chart
variant: helm
tags:
  - 0.1.6
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 49e03eecc1f19b88831e3e8eba537611bfcf0e6d
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:bf8a03f405f8a317925ae232b32a1d29943a57b019dabfb0b7d0e624f558baf2
  KUBECTL_IMAGE_REFERENCE: dhi/kubectl:1.35.4-debian13-compat@sha256:4e8802d2105a97f0332b443fe959fbc0595a693d82eea48df2542faf2f150a62
  KYVERNO_REPORTS_SERVER_IMAGE_REFERENCE: dhi/kyverno-reports-server:0.1.6-debian13@sha256:25b819642afb445c93ce98a21ead418f536bda12ef70034f1b74b79b99829b97
  SEMVER_MAJOR_MINOR_VERSION: "0.1"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.1.6
  VERSION: 0.1.6
contents:
  builds:
    - name: kyverno-reports-server-chart
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:bf8a03f405f8a317925ae232b32a1d29943a57b019dabfb0b7d0e624f558baf2
      contents:
        files:
          - url: git+https://github.com/kyverno/reports-server.git#reports-server-chart-0.1.6
            checksum: 49e03eecc1f19b88831e3e8eba537611bfcf0e6d
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            chart:
              appRef: kyverno-reports-server
              dependencies: null
              description: Docker Hardened Images Helm chart for Kyverno Reports Server
              images:
                - name: kyverno-reports-server
                  ref: dhi/kyverno-reports-server:0.1.6-debian13@sha256:25b819642afb445c93ce98a21ead418f536bda12ef70034f1b74b79b99829b97
                - name: kubectl
                  ref: dhi/kubectl:1.35.4-debian13-compat@sha256:4e8802d2105a97f0332b443fe959fbc0595a693d82eea48df2542faf2f150a62
              name: kyverno-reports-server-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kyverno/reports-server
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update chart dependencies
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            file: Chart.yaml
            patches:
              - del(.dependencies[] | select(.name == "postgresql"))
        - name: update values
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            file: values.yaml
            patches:
              - del(.postgresql)
        - name: helm login
          uses: helm/login@v1
          with:
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: reports-server
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: reports-server
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: reports-server
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kyverno-reports-server/config
