# syntax=dhi.io/build:2-helm3

name: Kyverno Reports Server Helm chart 0.x
image: dhi.io/kyverno-reports-server-chart
variant: helm
tags:
  - 0.1.7
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: d2b451bf72c175f25a653265a03002de7c0e0d3c
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:ed0c764ca91c1084c850f3b00e7248f9127153bb3a5803058a337ac99ba6d479
  KUBECTL_IMAGE_REFERENCE: dhi/kubectl:1.36.1-debian13-compat@sha256:de1971258de953023039cba856f1155a25ba3eff8d167dcf6ea64d39a0777c15
  KYVERNO_REPORTS_SERVER_IMAGE_REFERENCE: dhi/kyverno-reports-server:0.1.7-debian13@sha256:840d1ff4d80a56cf800558c1d315a542ddb21d7ef7e948f9c22d9a78d8af541d
  SEMVER_MAJOR_MINOR_VERSION: "0.1"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.1.7
  VERSION: 0.1.7
contents:
  builds:
    - name: kyverno-reports-server-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:ed0c764ca91c1084c850f3b00e7248f9127153bb3a5803058a337ac99ba6d479
      contents:
        files:
          - url: git+https://github.com/kyverno/reports-server.git#reports-server-chart-0.1.7
            checksum: d2b451bf72c175f25a653265a03002de7c0e0d3c
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            chart:
              appRef: kyverno-reports-server
              dependencies: null
              description: Docker Hardened Images Helm chart for Kyverno Reports Server
              images:
                - name: kyverno-reports-server
                  ref: dhi/kyverno-reports-server:0.1.7-debian13@sha256:840d1ff4d80a56cf800558c1d315a542ddb21d7ef7e948f9c22d9a78d8af541d
                - name: kubectl
                  ref: dhi/kubectl:1.36.1-debian13-compat@sha256:de1971258de953023039cba856f1155a25ba3eff8d167dcf6ea64d39a0777c15
              name: kyverno-reports-server-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kyverno/reports-server
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update chart dependencies
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            file: Chart.yaml
            patches:
              - del(.dependencies[] | select(.name == "postgresql"))
        - name: update values
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/charts/reports-server
          with:
            file: values.yaml
            patches:
              - del(.postgresql)
        - name: helm login
          uses: helm/login@v1
          with:
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: reports-server
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: reports-server
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: reports-server
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kyverno-reports-server/config
