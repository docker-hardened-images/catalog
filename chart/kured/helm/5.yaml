# syntax=dhi.io/build:2-helm3

name: Kured Helm chart 5.x
image: dhi.io/kured-chart
variant: helm
tags:
  - 5.11.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: cbd82b5ab365d91c65794febaa344d8db683948f
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
  KURED_IMAGE_REFERENCE: dhi/kured:1.21.0-debian13@sha256:51dad6c55f35912f9b934061be2a2a7b859b7d71c7b49b4a99eeb2e3aa04820d
  SEMVER_MAJOR_MINOR_VERSION: "5.11"
  SEMVER_MAJOR_VERSION: "5"
  SEMVER_VERSION: 5.11.0
  VERSION: 5.11.0
contents:
  builds:
    - name: kured-chart
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
      work-dir: ${source.dir}/helm-charts/charts/kured
      contents:
        files:
          - url: git+https://github.com/kubereboot/charts.git#kured-5.11.0
            checksum: cbd82b5ab365d91c65794febaa344d8db683948f
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: kured
              description: Docker Hardened Images Helm chart for Kured
              images:
                - name: kured
                  ref: dhi/kured:1.21.0-debian13@sha256:51dad6c55f35912f9b934061be2a2a7b859b7d71c7b49b4a99eeb2e3aa04820d
              name: kured-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubereboot/kured
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kured/config
