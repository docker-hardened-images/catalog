# syntax=dhi.io/build:2-helm3

name: Kured Helm chart 5.x
image: dhi.io/kured-chart
variant: helm
tags:
  - 5.12.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: ec2cb7e325854eefc52a5d0d4ec1cef256465c28
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:6506c914dff9353ce03ce64c8d30089cf14419d8d39f026c53917e1451fb8b21
  KURED_IMAGE_REFERENCE: dhi/kured:1.22.0-debian13@sha256:360208a3eaa6d906dd86e18825be295943e3d61348a72e7464b4408edaa78654
  SEMVER_MAJOR_MINOR_VERSION: "5.12"
  SEMVER_MAJOR_VERSION: "5"
  SEMVER_VERSION: 5.12.0
  VERSION: 5.12.0
contents:
  builds:
    - name: kured-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:6506c914dff9353ce03ce64c8d30089cf14419d8d39f026c53917e1451fb8b21
      work-dir: ${source.dir}/helm-charts/charts/kured
      contents:
        files:
          - url: git+https://github.com/kubereboot/charts.git#kured-5.12.0
            checksum: ec2cb7e325854eefc52a5d0d4ec1cef256465c28
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: kured
              description: Docker Hardened Images Helm chart for Kured
              images:
                - name: kured
                  ref: dhi/kured:1.22.0-debian13@sha256:360208a3eaa6d906dd86e18825be295943e3d61348a72e7464b4408edaa78654
              name: kured-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubereboot/kured
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kured/config
