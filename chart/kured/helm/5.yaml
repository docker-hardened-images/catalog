# syntax=dhi.io/build:2-helm3

name: Kured Helm chart 5.x
image: dhi.io/kured-chart
variant: helm
tags:
  - 5.11.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: cbd82b5ab365d91c65794febaa344d8db683948f
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:dde1e16cc0ad3d2cdeebdb78f1e676b899e6f62a12c529dd620d045bd2fd334a
  KURED_IMAGE_REFERENCE: dhi/kured:1.21.0-debian13@sha256:a9c4eb75591bcaa1a4340bdcc3dedc9fed5a2e9e729649f2d9383f6b15926e64
  SEMVER_MAJOR_MINOR_VERSION: "5.11"
  SEMVER_MAJOR_VERSION: "5"
  SEMVER_VERSION: 5.11.0
  VERSION: 5.11.0
contents:
  builds:
    - name: kured-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:dde1e16cc0ad3d2cdeebdb78f1e676b899e6f62a12c529dd620d045bd2fd334a
      work-dir: ${source.dir}/helm-charts/charts/kured
      contents:
        files:
          - url: git+https://github.com/kubereboot/charts.git#kured-5.11.0
            checksum: cbd82b5ab365d91c65794febaa344d8db683948f
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: kured
              description: Docker Hardened Images Helm chart for Kured
              images:
                - name: kured
                  ref: dhi/kured:1.21.0-debian13@sha256:a9c4eb75591bcaa1a4340bdcc3dedc9fed5a2e9e729649f2d9383f6b15926e64
              name: kured-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubereboot/kured
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kured/config
