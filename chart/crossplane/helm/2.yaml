# syntax=dhi.io/build:2-helm3

name: Crossplane Helm chart 2.x
image: dhi.io/crossplane-chart
variant: helm
tags:
  - 2.2.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 713541df7fc5cf0946b6573837831086465a2dbe
  CROSSPLANE_REFERENCE: dhi/crossplane:2.2.0-debian13@sha256:a6beb240a2d1dee9100505c081207af0c9153dd7bb51c918242e8dde5c3e175c
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:d3fce3e86c736f8a0d6006a879b3b3eb64074766065f62bcd67cef3c0fa6b848
  SEMVER_MAJOR_MINOR_VERSION: "2.2"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.2.1
  VERSION: 2.2.1
contents:
  builds:
    - name: crossplane
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:d3fce3e86c736f8a0d6006a879b3b3eb64074766065f62bcd67cef3c0fa6b848
      contents:
        files:
          - url: git+https://github.com/crossplane/crossplane.git#v2.2.1
            checksum: 713541df7fc5cf0946b6573837831086465a2dbe
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            chart:
              appRef: crossplane
              description: Docker Hardened Images Helm chart for Crossplane
              images:
                - name: crossplane
                  ref: dhi/crossplane:2.2.0-debian13@sha256:a6beb240a2d1dee9100505c081207af0c9153dd7bb51c918242e8dde5c3e175c
              name: crossplane-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/crossplane/crossplane
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts
          with:
            chart: crossplane
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/crossplane/config
