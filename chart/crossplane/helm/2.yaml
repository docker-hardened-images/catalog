# syntax=dhi.io/build:2-helm3

name: Crossplane Helm chart 2.x
image: dhi.io/crossplane-chart
variant: helm
tags:
  - 2.3.3
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 09ffaea39ccaea0f80817e35b5bbd3632b4e7e0d
  CHART_HELM_NAME: crossplane-chart
  CROSSPLANE_REFERENCE: dhi/crossplane:2.3.3-debian13@sha256:c3d526c12457a0a1160404729b727f1442b296316d7825e07760f841c328e96e
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.2-debian13-dev@sha256:e30b522f3f39307b7710b304f082f76d7b8fc0d0140bfc44bdf2892c5411519f
  SEMVER_MAJOR_MINOR_VERSION: "2.3"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.3.3
  VERSION: 2.3.3
contents:
  builds:
    - name: crossplane
      uses: dhi.io/helm:3.21.2-debian13-dev@sha256:e30b522f3f39307b7710b304f082f76d7b8fc0d0140bfc44bdf2892c5411519f
      contents:
        files:
          - url: git+https://github.com/crossplane/crossplane.git#v2.3.3
            checksum: 09ffaea39ccaea0f80817e35b5bbd3632b4e7e0d
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            chart:
              appRef: crossplane
              description: Docker Hardened Images Helm chart for Crossplane
              images:
                - name: crossplane
                  ref: dhi/crossplane:2.3.3-debian13@sha256:c3d526c12457a0a1160404729b727f1442b296316d7825e07760f841c328e96e
              name: crossplane-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/crossplane/crossplane
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts
          with:
            chart: crossplane
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/crossplane/config
