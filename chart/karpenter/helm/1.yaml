# syntax=dhi.io/build:2-helm3

name: Karpenter Helm chart 1.x
image: dhi.io/karpenter-chart
variant: helm
tags:
  - 1.12.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 7ff7943a971ba3df0ff755677fd76c13b11afef0
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:bf8a03f405f8a317925ae232b32a1d29943a57b019dabfb0b7d0e624f558baf2
  KARPENTER_REFERENCE: dhi/karpenter:1.9.0-debian13@sha256:7fd33231b13c2b27c2cf081dc4ff6313269414bee73dd8f5766eb30abc6d8274
  SEMVER_MAJOR_MINOR_VERSION: "1.12"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.12.0
  VERSION: 1.12.0
contents:
  builds:
    - name: karpenter
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:bf8a03f405f8a317925ae232b32a1d29943a57b019dabfb0b7d0e624f558baf2
      contents:
        files:
          - url: git+https://github.com/aws/karpenter-provider-aws.git#v1.12.0
            checksum: 7ff7943a971ba3df0ff755677fd76c13b11afef0
            path: ${source.dir}/karpenter-repo
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/karpenter-repo/charts/karpenter
          with:
            chart:
              appRef: karpenter
              description: Docker Hardened Images Helm chart for Karpenter
              images:
                - name: karpenter
                  ref: dhi/karpenter:1.9.0-debian13@sha256:7fd33231b13c2b27c2cf081dc4ff6313269414bee73dd8f5766eb30abc6d8274
              name: karpenter-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/aws/karpenter-provider-aws
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/karpenter-repo/charts/karpenter
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/karpenter-repo/charts/karpenter
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/karpenter-repo/charts
          with:
            chart: karpenter
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/karpenter-repo/charts/karpenter
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/karpenter/config
