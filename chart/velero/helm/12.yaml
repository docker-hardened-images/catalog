# syntax=dhi.io/build:2-helm3

name: Velero Helm chart 12.x
image: dhi.io/velero-chart
variant: helm
tags:
  - 12.0.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 4f79f7e4e68f72166c73001ebc72b65917131548
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:e3d7fb6de37608485fd22d3dd04ed6b0f9cc68431cb21697c2383978e2847ed2
  KUBECTL_REFERENCE: dhi/kubectl:1.35.5-debian13-compat@sha256:7f47dda88b8e98d6d5c79e1bec5c0dcd084693d8f9ee160eef03e9d9640b5d01
  SEMVER_MAJOR_MINOR_VERSION: "12.0"
  SEMVER_MAJOR_VERSION: "12"
  SEMVER_VERSION: 12.0.1
  VELERO_REFERENCE: dhi/velero:1.18.1-debian13@sha256:7d853650783065889a8fa8753ff81b0c4922a3b3fc0e236f4956f95d0937135b
  VERSION: 12.0.1
contents:
  builds:
    - name: velero
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:e3d7fb6de37608485fd22d3dd04ed6b0f9cc68431cb21697c2383978e2847ed2
      contents:
        files:
          - url: git+https://github.com/vmware-tanzu/helm-charts.git#velero-12.0.1
            checksum: 4f79f7e4e68f72166c73001ebc72b65917131548
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/velero
          with:
            chart:
              appRef: velero
              description: Docker Hardened Images Helm chart for Velero
              images:
                - name: velero
                  ref: dhi/velero:1.18.1-debian13@sha256:7d853650783065889a8fa8753ff81b0c4922a3b3fc0e236f4956f95d0937135b
                - name: kubectl
                  ref: dhi/kubectl:1.35.5-debian13-compat@sha256:7f47dda88b8e98d6d5c79e1bec5c0dcd084693d8f9ee160eef03e9d9640b5d01
              name: velero-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/vmware-tanzu/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/velero
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            process-templates: true
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/velero
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: velero
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/velero
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/velero/config
