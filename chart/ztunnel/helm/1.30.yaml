# syntax=dhi.io/build:2-helm3

name: Istio ZTunnel Helm chart 1.x
image: dhi.io/ztunnel-chart
variant: helm
tags:
  - 1.30.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 10229c76f2854420eeac94906ffff949b9aab746
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.1-debian13-dev@sha256:210cf8f74216e3b27dc70a0cec01194863ce8b319fe089d97e6e10d8a020b666
  SEMVER_MAJOR_MINOR_VERSION: "1.30"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.30.1
  VERSION: 1.30.1
  ZTUNNEL_REFERENCE: dhi/ztunnel:1.30.1-debian13@sha256:640cd941ad8e3c4d4e154a7e70c5b19b5f77546024a131a6858dace97321fae8
contents:
  builds:
    - name: ztunnel
      uses: dhi.io/helm:3.21.1-debian13-dev@sha256:210cf8f74216e3b27dc70a0cec01194863ce8b319fe089d97e6e10d8a020b666
      contents:
        files:
          - url: git+https://github.com/istio/istio.git#1.30.1
            checksum: 10229c76f2854420eeac94906ffff949b9aab746
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts/ztunnel
          with:
            chart:
              appRef: ztunnel
              description: Docker Hardened Images Helm chart for ZTunnel
              images:
                - name: ztunnel
                  ref: dhi/ztunnel:1.30.1-debian13@sha256:640cd941ad8e3c4d4e154a7e70c5b19b5f77546024a131a6858dace97321fae8
              name: ztunnel-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/istio/istio
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts/ztunnel
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/manifests/charts/ztunnel
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts
          with:
            chart: ztunnel
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/manifests/charts/ztunnel
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/ztunnel/config
