# syntax=dhi.io/build:2-helm3

name: APISIX Ingress Controller Helm chart 1.x
image: dhi.io/apisix-ingress-controller-chart
variant: helm
tags:
  - 1.2.1
platforms:
  - linux/amd64
vars:
  ADC_IMAGE_REFERENCE: dhi/adc:0.27.1-debian13@sha256:34f384fb5213e9990072b39072769fefde378f73104baf7dd82ec7dbeb475593
  APISIX_INGRESS_CONTROLLER_IMAGE_REFERENCE: dhi/apisix-ingress-controller:2.1.0-debian13@sha256:c1b1140bd01356029c1f09cf9e5aaf1d09ec5fa04fbafcd8f2a729ecdf910188
  CHART_COMMIT_SHA: d8fb74b08c73cb84d8b17901d3636243db5ba6c8
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "1.2"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.2.1
  VERSION: 1.2.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: apisix-ingress-controller-chart
      contents:
        packages:
          - bash
          - coreutils
          - sed
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/apache/apisix-helm-chart.git#apisix-ingress-controller-1.2.1
            checksum: d8fb74b08c73cb84d8b17901d3636243db5ba6c8
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/apisix-ingress-controller
          with:
            chart:
              appRef: apisix-ingress-controller
              description: Docker Hardened Images Helm chart for APISIX Ingress Controller
              images:
                - name: apisix-ingress-controller
                  ref: dhi/apisix-ingress-controller:2.1.0-debian13@sha256:c1b1140bd01356029c1f09cf9e5aaf1d09ec5fa04fbafcd8f2a729ecdf910188
                - name: adc
                  ref: dhi/adc:0.27.1-debian13@sha256:34f384fb5213e9990072b39072769fefde378f73104baf7dd82ec7dbeb475593
              name: apisix-ingress-controller-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/apache/apisix-ingress-controller
            cmd: prepare
            registry: dhi.io
        - name: add image pull secrets
          work-dir: ${source.dir}/helm-charts/charts/apisix-ingress-controller
          runs: |
            set -eux -o pipefail

            sed -i '/^      containers:/i\
                  {{- with .Values.imagePullSecrets }}\
                  imagePullSecrets:\
                    {{- range $.Values.imagePullSecrets }}\
                    - name: {{ . }}\
                    {{- end }}\
                  {{- end }}' templates/deployment.yaml
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/apisix-ingress-controller
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: apisix-ingress-controller
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: apisix-ingress-controller
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/apisix-ingress-controller/config
