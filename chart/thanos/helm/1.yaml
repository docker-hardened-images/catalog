# syntax=dhi.io/build:2-helm3

name: Thanos Helm chart 0.x
image: dhi.io/thanos-chart
variant: helm
tags:
  - 0.39.2
platforms:
  - linux/amd64
vars:
  BASH_REFERENCE: dhi/bash:5.2.37-debian13@sha256:9b098403cbfb7c1ffae85b6f997a81f2068cc817dc4ac90d8dc4305ef93c7bf3
  CHART_COMMIT_SHA: cd04b0e93380dcb987c52a214b20cd6864362d9d
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:382206314efea5932384da4cbb019ef72b83d65f2cc8fb2ba0acc7e8a31e5cf8
  SEMVER_MAJOR_MINOR_VERSION: "0.39"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.39.2
  THANOS_REFERENCE: dhi/thanos:0.41.0-debian13@sha256:521a605e1290ee745f561fc16dcbd5d0dd83ad4f5c2972f864563e3123936c09
  VERSION: 0.39.2
contents:
  builds:
    - name: thanos
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:382206314efea5932384da4cbb019ef72b83d65f2cc8fb2ba0acc7e8a31e5cf8
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/docker-hardened-images/helm-charts.git#thanos/0.39.2
            checksum: cd04b0e93380dcb987c52a214b20cd6864362d9d
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/thanos
          with:
            chart:
              appRef: thanos
              description: Docker Hardened Images Helm chart for Thanos
              images:
                - name: thanos
                  ref: dhi/thanos:0.41.0-debian13@sha256:521a605e1290ee745f561fc16dcbd5d0dd83ad4f5c2972f864563e3123936c09
                - name: bash
                  ref: dhi/bash:5.2.37-debian13@sha256:9b098403cbfb7c1ffae85b6f997a81f2068cc817dc4ac90d8dc4305ef93c7bf3
              name: thanos-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/docker-hardened-images/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/thanos
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update values
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/thanos
          with:
            file: values.yaml
            patches:
              - del(.minio)
        - name: update values
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/thanos
          with:
            file: Chart.yaml
            patches:
              - del(.dependencies.0)
        - name: update dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/thanos
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: thanos
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: thanos
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/thanos/config
