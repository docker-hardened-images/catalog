# syntax=dhi.io/build:2-helm3

name: Sealed Secrets Helm chart 0.x
image: dhi.io/sealed-secrets-chart
variant: helm
tags:
  - 0.36.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 94bd9538c8770a5a051d5c0d5f9936f968933dad
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.1-debian13-dev@sha256:a2c1b2e1c9d947d8feabde6c5ae3d2f9b1f0883a4d6cd01d49ccb0e509465920
  SEALED_SECRETS_REFERENCE: dhi/sealed-secrets-controller:0.36.1-debian13@sha256:7178a8fc094f0282b87f1b8b27e0c046af162f2abf3b2f3c5b50088fa4204f41
  SEMVER_MAJOR_MINOR_VERSION: "0.36"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.36.1
  VERSION: 0.36.1
contents:
  builds:
    - name: sealed-secrets-chart
      uses: dhi.io/helm:3.20.1-debian13-dev@sha256:a2c1b2e1c9d947d8feabde6c5ae3d2f9b1f0883a4d6cd01d49ccb0e509465920
      contents:
        files:
          - url: git+https://github.com/bitnami-labs/sealed-secrets.git#v0.36.1
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/helm/sealed-secrets
          with:
            chart:
              appRef: sealed-secrets-controller
              description: Docker Hardened Images Helm chart for Sealed Secrets
              images:
                - name: sealed-secrets-controller
                  ref: dhi/sealed-secrets-controller:0.36.1-debian13@sha256:7178a8fc094f0282b87f1b8b27e0c046af162f2abf3b2f3c5b50088fa4204f41
              name: sealed-secrets-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/bitnami-labs/sealed-secrets
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/helm/sealed-secrets
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          secrets:
            - id: docker_hub_user
              env: DOCKER_HUB_USER
            - id: docker_hub_password
              env: DOCKER_HUB_PASSWORD
          with:
            chart: ${source.dir}/helm-charts/helm/sealed-secrets
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/helm
          with:
            chart: sealed-secrets
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/helm/sealed-secrets
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/sealed-secrets/config
