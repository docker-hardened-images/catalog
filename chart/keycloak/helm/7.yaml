# syntax=dhi.io/build:2-helm3

name: Keycloak Helm chart 7.x
image: dhi.io/keycloak-chart
variant: helm
tags:
  - 7.2.0
platforms:
  - linux/amd64
vars:
  BUSYBOX_REFERENCE: dhi/busybox:1.37.0-alpine3.24@sha256:0c39435f09795b01feea9536a5a702bad2bdbd1e9a37df874fbfde4a0507d6ae
  CHART_COMMIT_SHA: 279061024a68a250bc6767737f3c1bf6e7539313
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.1-debian13-dev@sha256:a388778ec44cba5d2b949565c7974f48bd32509b795cff1650b86cb59734f43b
  KEYCLOAK_REFERENCE: dhi/keycloak:26.6.3-debian13@sha256:46eb216744e8520c3e165d091fd2fd7eba39cdfe401a12c22ebe786ac1a32148
  SEMVER_MAJOR_MINOR_VERSION: "7.2"
  SEMVER_MAJOR_VERSION: "7"
  SEMVER_VERSION: 7.2.0
  VERSION: 7.2.0
contents:
  builds:
    - name: keycloak-chart
      uses: dhi.io/helm:3.21.1-debian13-dev@sha256:a388778ec44cba5d2b949565c7974f48bd32509b795cff1650b86cb59734f43b
      contents:
        files:
          - url: git+https://github.com/codecentric/helm-charts.git#keycloakx-7.2.0
            checksum: 279061024a68a250bc6767737f3c1bf6e7539313
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            chart:
              appRef: keycloak
              description: Docker Hardened Images Helm chart for Keycloak
              images:
                - name: keycloak
                  ref: dhi/keycloak:26.6.3-debian13@sha256:46eb216744e8520c3e165d091fd2fd7eba39cdfe401a12c22ebe786ac1a32148
                - name: busybox
                  ref: dhi/busybox:1.37.0-alpine3.24@sha256:0c39435f09795b01feea9536a5a702bad2bdbd1e9a37df874fbfde4a0507d6ae
              name: keycloak-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/codecentric/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/keycloak/config
