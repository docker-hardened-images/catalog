# syntax=dhi.io/build:2-helm3

name: Keycloak Helm chart 7.x
image: dhi.io/keycloak-chart
variant: helm
tags:
  - 7.2.2
platforms:
  - linux/amd64
vars:
  BUSYBOX_REFERENCE: dhi/busybox:1.38.0-alpine3.24@sha256:69a25015bda2c7dfac5d3a88990b56bc0f38539b313c448b171edef1497193ad
  CHART_COMMIT_SHA: 52b0b7136d1b9e53bce61bef621c0392643e75f6
  CHART_HELM_NAME: keycloak-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KEYCLOAK_REFERENCE: dhi/keycloak:26.6.4-debian13@sha256:2aed1f6522bc659a1543f8b2caa2b888c890ba7bda5719d228576a88317e42ef
  SEMVER_MAJOR_MINOR_VERSION: "7.2"
  SEMVER_MAJOR_VERSION: "7"
  SEMVER_VERSION: 7.2.2
  VERSION: 7.2.2
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: keycloak-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/codecentric/helm-charts.git#keycloakx-7.2.2
            checksum: 52b0b7136d1b9e53bce61bef621c0392643e75f6
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            chart:
              appRef: keycloak
              description: Docker Hardened Images Helm chart for Keycloak
              images:
                - name: keycloak
                  ref: dhi/keycloak:26.6.4-debian13@sha256:2aed1f6522bc659a1543f8b2caa2b888c890ba7bda5719d228576a88317e42ef
                - name: busybox
                  ref: dhi/busybox:1.38.0-alpine3.24@sha256:69a25015bda2c7dfac5d3a88990b56bc0f38539b313c448b171edef1497193ad
              name: keycloak-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/codecentric/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/keycloak/config
