# syntax=dhi.io/build:2-helm3

name: Istio Gateway Helm chart 1.30.x
image: dhi.io/istio-gateway-chart
variant: helm
tags:
  - 1.30.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 10229c76f2854420eeac94906ffff949b9aab746
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.1-debian13-dev@sha256:a388778ec44cba5d2b949565c7974f48bd32509b795cff1650b86cb59734f43b
  SEMVER_MAJOR_MINOR_VERSION: "1.30"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.30.1
  VERSION: 1.30.1
contents:
  builds:
    - name: istio-gateway
      uses: dhi.io/helm:3.21.1-debian13-dev@sha256:a388778ec44cba5d2b949565c7974f48bd32509b795cff1650b86cb59734f43b
      work-dir: ${source.dir}/helm-charts/manifests/charts/gateway
      contents:
        files:
          - url: git+https://github.com/istio/istio.git#1.30.1
            checksum: 10229c76f2854420eeac94906ffff949b9aab746
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              description: Docker Hardened Images Helm chart for Istio Gateway
              name: istio-gateway-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/istio/istio/tree/master/manifests/charts/gateway
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/istio-gateway/config
