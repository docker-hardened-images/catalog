# syntax=dhi.io/build:2-helm3

name: Istio Base Helm chart 1.28.x
image: dhi.io/istio-base-chart
variant: helm
tags:
  - 1.28.8
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 07e4afa45ead54de23687a30c0730f50a5261065
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
  SEMVER_MAJOR_MINOR_VERSION: "1.28"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.28.8
  VERSION: 1.28.8
contents:
  builds:
    - name: istio-base-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
      contents:
        files:
          - url: git+https://github.com/istio/istio.git#07e4afa45ead54de23687a30c0730f50a5261065
            checksum: 07e4afa45ead54de23687a30c0730f50a5261065
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts/base
          with:
            chart:
              description: Docker Hardened Images Helm chart for Istio Base
              name: istio-base-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/istio/istio/tree/master/manifests/charts/base
            cmd: prepare
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts
          with:
            chart: base
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts
          with:
            chart: base
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/istio-base/config
