# syntax=dhi.io/build:2-helm3

name: Istio CNI Helm chart 1.30.x
image: dhi.io/istio-cni-chart
variant: helm
tags:
  - 1.30.2
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: f888ab4c8a0c3d0427bb0053fbebbe6b1f7e9a91
  CHART_HELM_NAME: istio-cni-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  ISTIO_INSTALL_CNI_REFERENCE: dhi/istio-install-cni:1.30.2-debian13@sha256:fda829d71a984363b5175962e5fe37b50c699903270bbd9a558a5f978b337fe7
  SEMVER_MAJOR_MINOR_VERSION: "1.30"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.30.2
  VERSION: 1.30.2
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: istio-cni
      work-dir: ${source.dir}/helm-charts/manifests/charts/istio-cni
      contents:
        packages:
          - bash
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/istio/istio.git#1.30.2
            checksum: f888ab4c8a0c3d0427bb0053fbebbe6b1f7e9a91
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: istio-install-cni
              description: Docker Hardened Images Helm chart for Istio CNI
              images:
                - name: istio-install-cni
                  ref: dhi/istio-install-cni:1.30.2-debian13@sha256:fda829d71a984363b5175962e5fe37b50c699903270bbd9a558a5f978b337fe7
              name: istio-cni-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/istio/istio
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/istio-cni/config
