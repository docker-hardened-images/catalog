# syntax=dhi.io/build:2-helm3

name: OpenSearch Dashboards Helm chart 3.x
image: dhi.io/opensearch-dashboards-chart
variant: helm
tags:
  - 3.6.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 0a2cba40e9733005b967cbef7adad2a39d361b72
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:9aa76ec9c4fe5fc42cb8182dd75fcde48f6a37a8180449cbd1cad7d66a7cc1de
  OPENSEARCH_DASHBOARDS_REFERENCE: dhi/opensearch-dashboards:3.6.0-debian13@sha256:cc6246c730a645f31be1828fdf34a12568c3e93647406668ed6cc50be1122f94
  SEMVER_MAJOR_MINOR_VERSION: "3.6"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.6.0
  VERSION: 3.6.0
contents:
  builds:
    - name: opensearch-dashboards
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:9aa76ec9c4fe5fc42cb8182dd75fcde48f6a37a8180449cbd1cad7d66a7cc1de
      contents:
        files:
          - url: git+https://github.com/opensearch-project/helm-charts.git#opensearch-dashboards-3.6.0
            checksum: 0a2cba40e9733005b967cbef7adad2a39d361b72
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            chart:
              appRef: opensearch-dashboards
              description: Docker Hardened Images Helm chart for OpenSearch Dashboards
              images:
                - name: opensearch-dashboards
                  ref: dhi/opensearch-dashboards:3.6.0-debian13@sha256:cc6246c730a645f31be1828fdf34a12568c3e93647406668ed6cc50be1122f94
              name: opensearch-dashboards-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/opensearch-project/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: opensearch-dashboards
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/opensearch-dashboards/config
