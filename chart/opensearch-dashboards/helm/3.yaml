# syntax=dhi.io/build:2-helm3

name: OpenSearch Dashboards Helm chart 3.x
image: dhi.io/opensearch-dashboards-chart
variant: helm
tags:
  - 3.7.0-1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 97cf162481e4f30d67c5648b1c2e3e87a02941bc
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.2-debian13-dev@sha256:e30b522f3f39307b7710b304f082f76d7b8fc0d0140bfc44bdf2892c5411519f
  OPENSEARCH_DASHBOARDS_REFERENCE: dhi/opensearch-dashboards:3.7.0-debian13@sha256:f51300567c5c0bd4696c1b7957321d744a427f63721fbcd2f3880bba9277209c
  SEMVER_MAJOR_MINOR_VERSION: "3.7"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.7.0
  VERSION: 3.7.0-1
contents:
  builds:
    - name: opensearch-dashboards
      uses: dhi.io/helm:3.21.2-debian13-dev@sha256:e30b522f3f39307b7710b304f082f76d7b8fc0d0140bfc44bdf2892c5411519f
      contents:
        files:
          - url: git+https://github.com/opensearch-project/helm-charts.git#opensearch-dashboards-3.7.0-1
            checksum: 97cf162481e4f30d67c5648b1c2e3e87a02941bc
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            chart:
              appRef: opensearch-dashboards
              description: Docker Hardened Images Helm chart for OpenSearch Dashboards
              images:
                - name: opensearch-dashboards
                  ref: dhi/opensearch-dashboards:3.7.0-debian13@sha256:f51300567c5c0bd4696c1b7957321d744a427f63721fbcd2f3880bba9277209c
              name: opensearch-dashboards-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/opensearch-project/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: opensearch-dashboards
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/opensearch-dashboards/config
