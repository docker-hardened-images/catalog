# syntax=dhi.io/build:2-helm3

name: Piraeus HA Controller Helm chart 1.x
image: dhi.io/piraeus-ha-controller-chart
variant: helm
tags:
  - 1.3.2
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 97d304aa1d2d67985517c78ed29953f9ec91bdbc
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:ed0c764ca91c1084c850f3b00e7248f9127153bb3a5803058a337ac99ba6d479
  PIRAEUS_HA_CONTROLLER_REFERENCE: dhi/piraeus-ha-controller:1.3.2-debian13@sha256:fb2aab033b7649c8bbec57a176aeb8b14fd1001df04d43d985a190f79e612a36
  SEMVER_MAJOR_MINOR_VERSION: "1.3"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.3.2
  VERSION: 1.3.2
contents:
  builds:
    - name: piraeus-ha-controller
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:ed0c764ca91c1084c850f3b00e7248f9127153bb3a5803058a337ac99ba6d479
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/piraeusdatastore/helm-charts.git#piraeus-ha-controller-1.3.2
            checksum: 97d304aa1d2d67985517c78ed29953f9ec91bdbc
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/piraeus-ha-controller
          with:
            chart:
              appRef: piraeus-ha-controller
              description: Docker Hardened Images Helm chart for Piraeus HA Controller
              images:
                - name: piraeus-ha-controller
                  ref: dhi/piraeus-ha-controller:1.3.2-debian13@sha256:fb2aab033b7649c8bbec57a176aeb8b14fd1001df04d43d985a190f79e612a36
              name: piraeus-ha-controller-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/piraeusdatastore/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/piraeus-ha-controller
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: piraeus-ha-controller
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: piraeus-ha-controller
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/piraeus-ha-controller/config
