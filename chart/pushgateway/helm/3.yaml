# syntax=dhi.io/build:2-helm3

name: Prometheus Pushgateway Helm chart 3.x
image: dhi.io/pushgateway-chart
variant: helm
tags:
  - 3.6.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: f0c2bc5208db855f2f8246cb97853c430ba0b6f7
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:e3d7fb6de37608485fd22d3dd04ed6b0f9cc68431cb21697c2383978e2847ed2
  PUSHGATEWAY_REFERENCE: dhi/pushgateway:1.11.2-debian13@sha256:3cd62dc8c7ae0bd0c071a3cd5542da789f68e361f26fca32b86f54e3a6134f07
  SEMVER_MAJOR_MINOR_VERSION: "3.6"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.6.1
  VERSION: 3.6.1
contents:
  builds:
    - name: prometheus-pushgateway-chart
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:e3d7fb6de37608485fd22d3dd04ed6b0f9cc68431cb21697c2383978e2847ed2
      contents:
        files:
          - url: git+https://github.com/prometheus-community/helm-charts.git#prometheus-pushgateway-3.6.1
            checksum: f0c2bc5208db855f2f8246cb97853c430ba0b6f7
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-pushgateway
          with:
            chart:
              appRef: pushgateway
              description: Docker Hardened Images Helm chart for Prometheus Pushgateway
              images:
                - name: pushgateway
                  ref: dhi/pushgateway:1.11.2-debian13@sha256:3cd62dc8c7ae0bd0c071a3cd5542da789f68e361f26fca32b86f54e3a6134f07
              name: pushgateway-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/prometheus-community/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-pushgateway
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: prometheus-pushgateway
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-pushgateway
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-pushgateway
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/pushgateway/config
