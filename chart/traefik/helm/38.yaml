# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 38.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 38.0.2
platforms:
  - linux/amd64
vars:
  COMMIT_SHA: 08588b97c8ea6ede92e86dcb750ec4bc4ad94e45
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:ce95680846fc18d3d179f82f9d633c8274f41cd0d0f8213cbfc4039fbe2cd112
  SEMVER_MAJOR_MINOR_VERSION: "38.0"
  SEMVER_MAJOR_VERSION: "38"
  SEMVER_VERSION: 38.0.2
  TRAEFIK_REFERENCE: dhi/traefik:3.6.16-debian13@sha256:a5364e774766fe4e09d8b4d91bd7add554c72229372313560e824c649c83fa7f
  VERSION: 38.0.2
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:ce95680846fc18d3d179f82f9d633c8274f41cd0d0f8213cbfc4039fbe2cd112
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v38.0.2
            checksum: 08588b97c8ea6ede92e86dcb750ec4bc4ad94e45
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.6.16-debian13@sha256:a5364e774766fe4e09d8b4d91bd7add554c72229372313560e824c649c83fa7f
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
