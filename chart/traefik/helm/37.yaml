# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 37.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 37.4.0
platforms:
  - linux/amd64
vars:
  COMMIT_SHA: 64d25f2c7dd932b3b7dc27d0bfa6ea131fc280f3
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
  SEMVER_MAJOR_MINOR_VERSION: "37.4"
  SEMVER_MAJOR_VERSION: "37"
  SEMVER_VERSION: 37.4.0
  TRAEFIK_REFERENCE: dhi/traefik:3.6.14-debian13@sha256:b71ffbb608f77f14c6b14290096670e9c2a51df5d8247710b8eac6ac4142025a
  VERSION: 37.4.0
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:f9dd4a356298b0769dc4c2f718b9f564b631ddf37c19e061d61af9081bc7da13
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v37.4.0
            checksum: 64d25f2c7dd932b3b7dc27d0bfa6ea131fc280f3
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.6.14-debian13@sha256:b71ffbb608f77f14c6b14290096670e9c2a51df5d8247710b8eac6ac4142025a
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
