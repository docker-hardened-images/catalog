# syntax=dhi/build:1-helm3

# DO NOT EDIT THIS FILE MANUALLY
# generated with 'dhi def merge chart/traefik/helm/37.yaml'

name: Traefik Helm chart 37.x
image: dhi/traefik-chart
variant: helm
tags:
    - 37.1.2
platforms:
    - linux/amd64
vars:
    CHART_COMMIT_SHA: 75bdb79d11e23560eebb0302f030121e5cfb081f
    DHI_NAMESPACE: dhi
    DHI_PREFIX: ""
    DHI_REGISTRY: docker.io
    DHI_SKIP_CONTEXT_COPY: "true"
    HELM_REFERENCE: dhi/helm:3.19.3-debian13-dev@sha256:e5f5727e6d74131d8d9201c70de54c9431a562b749ccf6af0c56acbb39d2d73a
    SEMVER_MAJOR_MINOR_VERSION: "37.1"
    SEMVER_MAJOR_VERSION: "37"
    SEMVER_VERSION: 37.1.2
    TRAEFIK_COMMIT_SHA: 4e441f8b183f60a3f5d82efbca18a7407318aeda
    TRAEFIK_REFERENCE: dhi/traefik:3.6.4-debian13@sha256:b14eafd43bb92c2790df5e5d2aae2ad793198ce36fe40edb17579dfbedff77cf
    TRAEFIK_VERSION: 3.6.4
    VERSION: 37.1.2
contents:
    builds:
        - name: traefik
          uses: dhi/helm:3.19.3-debian13-dev@sha256:e5f5727e6d74131d8d9201c70de54c9431a562b749ccf6af0c56acbb39d2d73a
          contents:
            files:
                - url: git+https://github.com/traefik/traefik-helm-chart.git#v37.1.2
                  path: ${source.dir}/helm-charts
                  uid: 0
                  gid: 0
          pipeline:
            - name: prepare chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/traefik
              with:
                chart:
                    appRef: traefik
                    description: Docker Hardened Images Helm chart for Traefik
                    images:
                        - name: traefik
                          ref: dhi/traefik:3.6.4-debian13@sha256:b14eafd43bb92c2790df5e5d2aae2ad793198ce36fe40edb17579dfbedff77cf
                    name: traefik-chart
                    notes: ${source.dir}/config/NOTES.txt
                    sourceUrl: https://github.com/traefik/traefik-helm-chart
                cmd: prepare
                registry: docker.io
            - name: relocate chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/traefik
              with:
                cmd: patch
                patches:
                    from: ${source.dir}/config
                registry: docker.io
            - name: update dependencies
              uses: helm/cmd@v1
              with:
                chart: ${source.dir}/helm-charts/traefik
                cmd: dependency update
            - name: lint
              uses: helm/cmd@v1
              work-dir: ${source.dir}/helm-charts
              with:
                chart: traefik
                cmd: lint .
            - name: package
              uses: helm/package@v1
              with:
                chart: ${source.dir}/helm-charts/traefik
          paths:
            - type: directory
              path: ${source.dir}/config
              uid: 0
              gid: 0
              mode: "0644"
              source: chart/traefik/config
tests:
    - name: Run traefik testcontainers helm tests
      directory: chart/traefik/tests
      commands:
        - go test . -count=1 -v
      exit-code: 0
    - name: Run base helm tests
      directory: test/go-testing/k8s/helm/base
      commands:
        - go test . -count=1 -v
      exit-code: 0
