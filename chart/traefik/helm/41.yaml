# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 41.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 41.0.0
platforms:
  - linux/amd64
vars:
  APPVERSION: 3.7.5
  COMMIT_SHA: 5023a1b2cb982e5ea18b5e915ac9a772f510a793
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.2-debian13-dev@sha256:2d07671ccc209a0a3e1082dcd0375511e57b8ad6a18439c74924f203d63a461c
  SEMVER_MAJOR_MINOR_VERSION: "41.0"
  SEMVER_MAJOR_VERSION: "41"
  SEMVER_VERSION: 41.0.0
  TRAEFIK_REFERENCE: dhi/traefik:3.7.5-debian13@sha256:e3f58ff89c6066cebc558380c417130518c58d5b0c9be3dbea31c0bc608f9539
  VERSION: 41.0.0
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.21.2-debian13-dev@sha256:2d07671ccc209a0a3e1082dcd0375511e57b8ad6a18439c74924f203d63a461c
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v41.0.0
            checksum: 5023a1b2cb982e5ea18b5e915ac9a772f510a793
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.7.5-debian13@sha256:e3f58ff89c6066cebc558380c417130518c58d5b0c9be3dbea31c0bc608f9539
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: restore upstream chart annotations
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            file: Chart.yaml
            patches:
              - eval(.annotations = (load("${source.dir}/config/chart-annotations.yaml") * (.annotations // {})))
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
