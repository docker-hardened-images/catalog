# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 39.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 39.0.9
platforms:
  - linux/amd64
vars:
  COMMIT_SHA: b138b95c398b9dd7065e736bc618913fb3fdcea5
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
  SEMVER_MAJOR_MINOR_VERSION: "39.0"
  SEMVER_MAJOR_VERSION: "39"
  SEMVER_VERSION: 39.0.9
  TRAEFIK_REFERENCE: dhi/traefik:3.7.5-debian13@sha256:6be8157ec8455ed845d7b7a48c97e7be98f0072bb7fabeb870dc9776d109640d
  VERSION: 39.0.9
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:ece8efeb889adab72837b0042d8419283ac7fdf2b5c2c8fe09a34e4b8a34ae2e
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v39.0.9
            checksum: b138b95c398b9dd7065e736bc618913fb3fdcea5
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.7.5-debian13@sha256:6be8157ec8455ed845d7b7a48c97e7be98f0072bb7fabeb870dc9776d109640d
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
