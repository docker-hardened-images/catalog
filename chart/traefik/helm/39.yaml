# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 39.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 39.0.8
platforms:
  - linux/amd64
vars:
  COMMIT_SHA: 13dc6ff47c50e5bc13d4d30d251e12b886b01e76
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.2-debian13-dev@sha256:d3fce3e86c736f8a0d6006a879b3b3eb64074766065f62bcd67cef3c0fa6b848
  SEMVER_MAJOR_MINOR_VERSION: "39.0"
  SEMVER_MAJOR_VERSION: "39"
  SEMVER_VERSION: 39.0.8
  TRAEFIK_REFERENCE: dhi/traefik:3.6.13-debian13@sha256:7b998c4eb78df7dc1e121c437dae15cf3bca6a4e3b7fade95c52ac6da49af994
  VERSION: 39.0.8
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.20.2-debian13-dev@sha256:d3fce3e86c736f8a0d6006a879b3b3eb64074766065f62bcd67cef3c0fa6b848
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v39.0.8
            checksum: 13dc6ff47c50e5bc13d4d30d251e12b886b01e76
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.6.13-debian13@sha256:7b998c4eb78df7dc1e121c437dae15cf3bca6a4e3b7fade95c52ac6da49af994
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
