# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 39.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 39.0.9
platforms:
  - linux/amd64
vars:
  APPVERSION: 3.6.15
  COMMIT_SHA: b138b95c398b9dd7065e736bc618913fb3fdcea5
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.2-debian13-dev@sha256:44218c50feae45f17602aa1b67db039b70881c9275057ce0647f6d50d3def16e
  SEMVER_MAJOR_MINOR_VERSION: "39.0"
  SEMVER_MAJOR_VERSION: "39"
  SEMVER_VERSION: 39.0.9
  TRAEFIK_REFERENCE: dhi/traefik:3.6.15-debian13@sha256:22d71d8fc3da512304a2426bf0c9111b918e0dbf77c3fa3ff36c503d37619964
  VERSION: 39.0.9
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.21.2-debian13-dev@sha256:44218c50feae45f17602aa1b67db039b70881c9275057ce0647f6d50d3def16e
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v39.0.9
            checksum: b138b95c398b9dd7065e736bc618913fb3fdcea5
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.6.15-debian13@sha256:22d71d8fc3da512304a2426bf0c9111b918e0dbf77c3fa3ff36c503d37619964
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: restore upstream chart annotations
          uses: yq/patch@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            file: Chart.yaml
            patches:
              - eval(.annotations = (load("${source.dir}/config/chart-annotations.yaml") * (.annotations // {})))
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
