# syntax=dhi.io/build:2-debian13

name: Open Policy Agent 1.x
image: dhi.io/open-policy-agent
variant: runtime
tags:
  - "1"
  - "1.16"
  - 1.16.1
  - 1-debian13
  - 1.16-debian13
  - 1.16.1-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2024-12-20"
vars:
  BASE_DIGEST: sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
  BASE_TAG: 20250419-debian13
  COMMIT_SHA: a72f9fa5e7c5e10a614f44dbe40700f90816d59d
  GOLANG_REFERENCE: dhi/golang:1.26.3-debian13-dev@sha256:fdab09a65b42748429d758c1b1d4b2b3e70432aba826445e855669aa815e90a6
  SEMVER_MAJOR_MINOR_VERSION: "1.16"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.16.1
  VERSION: 1.16.1
contents:
  packages:
    - '!base-files'
  builds:
    - name: open-policy-agent
      uses: dhi.io/golang:1.26.3-debian13-dev@sha256:fdab09a65b42748429d758c1b1d4b2b3e70432aba826445e855669aa815e90a6
      work-dir: /go/src/github.com/open-policy-agent
      caches:
        - path: /go/pkg/mod
      contents:
        files:
          - url: git+https://github.com/open-policy-agent/opa.git#v1.16.1
            checksum: a72f9fa5e7c5e10a614f44dbe40700f90816d59d
            path: /go/src/github.com/open-policy-agent
            spdx:
              name: open-policy-agent
              version: 1.16.1
              packages:
                - name: open-policy-agent
                  purl: pkg:dhi/open-policy-agent@1.16.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps:
              - go.opentelemetry.io/otel/sdk@v1.43.0
              - go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp@v1.43.0
            download: true
        - name: test
          privileged: true
          runs: |
            set -eux -o pipefail

            if [ "${target.fips}" = "true" ]; then
              # Use lenient mode for the build-time sanity step so Go can still fetch
              # modules during generate/test; the final FIPS image itself remains in
              # strict fips140=only mode.
              export GODEBUG=fips140=on
              export GOFIPS140=v1.0.0
            fi

            # Mirrors upstream's generate target before running unit tests and build.
            CGO_ENABLED=0 GOFLAGS="-buildmode=exe" go generate
            # Run a focused product-relevant Go unit-test subset during the image build;
            # the full image behavior is validated by the image test suite.
            CGO_ENABLED=0 GOFLAGS="-buildmode=exe" go test ./v1/rego ./v1/topdown ./v1/bundle ./v1/runtime ./v1/server/... ./v1/sdk
        - name: build
          uses: go/build@v1
          with:
            ldflags:
              - -X github.com/open-policy-agent/opa/version.Hostname=dhi
              - -X github.com/open-policy-agent/opa/version.Version=1.16.1
            output: ${target.dir}/usr/bin/opa
            packages: .
      outputs:
        - source: ${target.dir}/usr/bin/opa
          target: /usr/bin/opa
          uid: 0
          gid: 0
          mode: "0755"
        - source: /opt/docker/sbom/open-policy-agent
          target: /opt/docker/sbom/open-policy-agent
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/static:20250419-debian13@sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
      includes:
        - bin
        - etc
        - home
        - lib
        - lib64
        - opt
        - sbin
        - tmp
        - usr
        - var
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
annotations:
  org.opencontainers.image.description: A minimal Open Policy Agent image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/bin/opa
