metadata:
  display-name: Secrets Store CSI Driver CRDs
  short-description: Packages the Secrets Store CSI Driver CustomResourceDefinitions together with kubectl, so a cluster can register SecretProviderClass and SecretProviderClassPodStatus before the driver itself is deployed.
  featured: false
  home-url: https://secrets-store-csi-driver.sigs.k8s.io
  categories:
    - security
