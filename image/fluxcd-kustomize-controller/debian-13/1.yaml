# syntax=dhi.io/build:2-debian13

name: fluxcd kustomize controller 1.9.x
image: dhi.io/fluxcd-kustomize-controller
variant: runtime
tags:
  - "1"
  - "1.9"
  - 1.9.0
  - 1-debian
  - 1-debian13
  - 1.9-debian
  - 1.9-debian13
  - 1.9.0-debian
  - 1.9.0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2023-07-04"
vars:
  COMMIT_SHA: 54691381857d2b8d5714359bb8374c713ba6e9fb
  SEMVER_MAJOR_MINOR_VERSION: "1.9"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.9.0
  VERSION: 1.9.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - base-files
    - bash
    - ca-certificates-bundle
    - coreutils
    - tini
    - tzdata
  builds:
    - name: fluxcd-kustomize-controller
      work-dir: /go/src/github.com/fluxcd/kustomize-controller
      caches:
        - path: /go/pkg/mod
      contents:
        packages:
          - bash
          - ca-certificates-bundle
          - coreutils
          - golang-1.26
          - findutils
          - mawk
          - sed
        files:
          - url: git+https://github.com/fluxcd/kustomize-controller#v1.9.0
            checksum: 54691381857d2b8d5714359bb8374c713ba6e9fb
            path: /go/src/github.com/fluxcd/kustomize-controller
            spdx:
              name: fluxcd-kustomize-controller
              version: 1.9.0
              packages:
                - name: fluxcd-kustomize-controller
                  purl: pkg:golang/github.com/fluxcd/kustomize-controller@1.9.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: test
          runs: |
            set -eux -o pipefail

            # Exclude packages that cannot run without external tools or a live k8s API server:
            # - internal/controller: requires envtest (live k8s API server via etcd)
            # - internal/decryptor: TestDecryptor_ImportKeys requires gpg in $PATH
            # - internal/sops/keyservice: TestServer_EncryptDecrypt_PGP requires gpg in $PATH
            # CGO_ENABLED=0 go test $(go list ./internal/... | grep -vE '/internal/(controller|decryptor|sops/keyservice)')
        - name: build
          uses: go/build@v1
          environment:
            GOFLAGS: -buildvcs=false
          with:
            output: ${target.dir}/usr/local/bin/kustomize-controller
      outputs:
        - source: ${target.dir}/usr/local/bin/kustomize-controller
          target: /usr/local/bin/kustomize-controller
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/fluxcd-kustomize-controller
          target: /opt/docker/sbom/fluxcd-kustomize-controller
          uid: 0
          gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  GNUPGHOME: /tmp
annotations:
  org.opencontainers.image.description: A minimal Flux CD kustomize-controller image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - tini
  - --
  - /usr/local/bin/kustomize-controller
