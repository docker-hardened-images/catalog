# syntax=dhi.io/build:2-debian13

name: OpenSearch Dashboards 3.x
image: dhi.io/opensearch-dashboards
variant: runtime
tags:
  - "3"
  - "3.7"
  - 3.7.0
  - 3-debian
  - 3-debian13
  - 3.7-debian
  - 3.7-debian13
  - 3.7.0-debian
  - 3.7.0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-05-06"
vars:
  ALERTING_DASHBOARDS_COMMIT_SHA: f2d547f4c598a22f11cb1b59185ee59c55298b90
  ALERTING_DASHBOARDS_VERSION: 3.7.0.0
  ANOMALY_DETECTION_DASHBOARDS_COMMIT_SHA: fb3fe5f274c22bd1fa385af2c2fa614ac2e58f4e
  ANOMALY_DETECTION_DASHBOARDS_VERSION: 3.7.0.0
  ARCH: '#{ target.arch == "amd64" ? "amd64" : "arm64" }'
  ASSISTANT_DASHBOARDS_COMMIT_SHA: f55e69598b634f7557916dda69e185967256b67d
  ASSISTANT_DASHBOARDS_VERSION: 3.7.0.0
  CUSTOM_IMPORT_MAP_DASHBOARDS_COMMIT_SHA: 06cf56c1c3c34bb3df4c279e9aa22b9c23b10f2a
  CUSTOM_IMPORT_MAP_DASHBOARDS_VERSION: 3.7.0.0
  FLOW_FRAMEWORK_DASHBOARDS_COMMIT_SHA: 48357118314f67620218fcd9de0160ddeb84c3c8
  FLOW_FRAMEWORK_DASHBOARDS_VERSION: 3.7.0.0
  FUNCTIONAL_TEST_DASHBOARDS_COMMIT_SHA: fd3968d51257cafab31e5f8397bdf5a26dcabad7
  INDEX_MANAGEMENT_DASHBOARDS_COMMIT_SHA: 6b82c478554aed7f1345bc6c5199bcd803633d66
  INDEX_MANAGEMENT_DASHBOARDS_VERSION: 3.7.0.0
  INVESTIGATION_DASHBOARDS_COMMIT_SHA: ac622757fce1f59f989c74a589dbe13be1b49d13
  INVESTIGATION_DASHBOARDS_VERSION: 3.7.0.0
  ML_COMMONS_DASHBOARDS_COMMIT_SHA: ab8f82a3a30eff18491d7e103b9e866bd8d8d5b0
  ML_COMMONS_DASHBOARDS_VERSION: 3.7.0.0
  NOTIFICATIONS_DASHBOARDS_COMMIT_SHA: 07580bb339bf65b151a3417e4ada09ea1d433068
  NOTIFICATIONS_DASHBOARDS_VERSION: 3.7.0.0
  OBSERVABILITY_DASHBOARDS_COMMIT_SHA: 277f3ffc72038f65d8a1574c99b32b3a3549d2b8
  OBSERVABILITY_DASHBOARDS_VERSION: 3.7.0.0
  OPEN_SEARCH_DASHBOARDS_COMMIT_SHA: fbc260ec5a09d1c39270d20c9736f602ab4425d5
  OPENSEARCH_BUILD_ARCH: '#{ target.arch == "amd64" ? "linux" : "linux-arm" }'
  OPENSEARCH_BUILD_COMMIT_SHA: 18761b2bc0e7c224ff402da1380a5c6e56330f0f
  OPENSEARCH_BUILD_VERSION: 3.6.0
  OPENSEARCH_DASHBOARDS_ARCH: '#{ target.arch == "amd64" ? "x64" : "arm64" }'
  OPENSEARCH_DASHBOARDS_COMMIT_SHA: dd15c5757c758bc51d8992e979e05f975a605434
  OPENSEARCH_DASHBOARDS_MAJOR_MINOR_VERSION: "3.7"
  OPENSEARCH_DIST_ARCH: '#{ target.arch == "amd64" ? "linux-x64" : "linux-arm64" }'
  QUERY_INSIGHTS_DASHBOARDS_COMMIT_SHA: 1d4d0f8165fe5c872ebd3fb9472ec15e5742bfb6
  QUERY_INSIGHTS_DASHBOARDS_VERSION: 3.7.0.0
  QUERY_WORKBENCH_DASHBOARDS_COMMIT_SHA: 0b1f3f3bb1347540fb28836353e1c8e743577b84
  QUERY_WORKBENCH_DASHBOARDS_VERSION: 3.7.0.0
  REPORTS_DASHBOARDS_COMMIT_SHA: 186ad71c79955fbc28967d5f007bb8fc696d4b1c
  REPORTS_DASHBOARDS_VERSION: 3.7.0.0
  SEARCH_RELEVANCE_DASHBOARDS_COMMIT_SHA: 51f0996849421e1f743fe0500edd8536cddade81
  SEARCH_RELEVANCE_DASHBOARDS_VERSION: 3.7.0.0
  SECURITY_ANALYTICS_DASHBOARDS_COMMIT_SHA: 73f09aff73cfc63ab8b03c71335adaf57fab43f1
  SECURITY_ANALYTICS_DASHBOARDS_VERSION: 3.7.0.0
  SECURITY_DASHBOARDS_COMMIT_SHA: 8ec5b2b4fe005e8a2c90ec51c078e34dccf47e99
  SECURITY_DASHBOARDS_VERSION: 3.7.0.0
  SEMVER_MAJOR_MINOR_VERSION: "3.7"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.7.0
  VERSION: 3.7.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - bash
    - ca-certificates
    - ca-certificates-bundle
    - coreutils
    - findutils
    - grep
    - libc6
    - libgomp1
    - libstdc++6
    - netbase
    - nodejs-22
    - tzdata
  builds:
    - name: opensearch-dashboards from source
      caches:
        - path: /usr/local/share/.cache/yarn
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - gnupg
          - sed
          - tar
          - xz-utils
          - gzip
          - grep
          - unzip
          - base-files
          - ca-certificates
          - git
          - libc6
          - libstdc++6
          - mawk
          - nodejs-22
          - node-corepack
          - npm
          - yarn-classic
        files:
          - url: git+https://github.com/opensearch-project/OpenSearch-Dashboards.git#3.7.0
            checksum: dd15c5757c758bc51d8992e979e05f975a605434
            path: ${source.dir}/opensearch-dashboards
            spdx:
              name: opensearch-dashboards
              version: 3.7.0
              packages:
                - name: opensearch-dashboards
                  purl: pkg:dhi/opensearch-dashboards@3.7.0
                  license: Apache-2.0
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-observability.git#3.7.0.0
            checksum: 277f3ffc72038f65d8a1574c99b32b3a3549d2b8
            path: ${source.dir}/opensearch-dashboards/plugins/observabilityDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-reporting.git#3.7.0.0
            checksum: 186ad71c79955fbc28967d5f007bb8fc696d4b1c
            path: ${source.dir}/opensearch-dashboards/plugins/reportsDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-query-workbench.git#3.7.0.0
            checksum: 0b1f3f3bb1347540fb28836353e1c8e743577b84
            path: ${source.dir}/opensearch-dashboards/plugins/queryWorkbenchDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-maps.git#3.7.0.0
            checksum: 06cf56c1c3c34bb3df4c279e9aa22b9c23b10f2a
            path: ${source.dir}/opensearch-dashboards/plugins/customImportMapDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/anomaly-detection-dashboards-plugin.git#3.7.0.0
            checksum: fb3fe5f274c22bd1fa385af2c2fa614ac2e58f4e
            path: ${source.dir}/opensearch-dashboards/plugins/anomalyDetectionDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/ml-commons-dashboards.git#3.7.0.0
            checksum: ab8f82a3a30eff18491d7e103b9e866bd8d8d5b0
            path: ${source.dir}/opensearch-dashboards/plugins/mlCommonsDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-investigation.git#3.7.0.0
            checksum: ac622757fce1f59f989c74a589dbe13be1b49d13
            path: ${source.dir}/opensearch-dashboards/plugins/investigationDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/index-management-dashboards-plugin.git#3.7.0.0
            checksum: 6b82c478554aed7f1345bc6c5199bcd803633d66
            path: ${source.dir}/opensearch-dashboards/plugins/indexManagementDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-notifications.git#3.7.0.0
            checksum: 07580bb339bf65b151a3417e4ada09ea1d433068
            path: ${source.dir}/opensearch-dashboards/plugins/notificationsDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/alerting-dashboards-plugin.git#3.7.0.0
            checksum: f2d547f4c598a22f11cb1b59185ee59c55298b90
            path: ${source.dir}/opensearch-dashboards/plugins/alertingDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/security-analytics-dashboards-plugin.git#3.7.0.0
            checksum: 73f09aff73cfc63ab8b03c71335adaf57fab43f1
            path: ${source.dir}/opensearch-dashboards/plugins/securityAnalyticsDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/security-dashboards-plugin.git#3.7.0.0
            checksum: 8ec5b2b4fe005e8a2c90ec51c078e34dccf47e99
            path: ${source.dir}/opensearch-dashboards/plugins/securityDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-search-relevance.git#3.7.0.0
            checksum: 51f0996849421e1f743fe0500edd8536cddade81
            path: ${source.dir}/opensearch-dashboards/plugins/searchRelevanceDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-assistant.git#3.7.0.0
            checksum: f55e69598b634f7557916dda69e185967256b67d
            path: ${source.dir}/opensearch-dashboards/plugins/assistantDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/dashboards-flow-framework.git#3.7.0.0
            checksum: 48357118314f67620218fcd9de0160ddeb84c3c8
            path: ${source.dir}/opensearch-dashboards/plugins/flowFrameworkDashboards
            uid: 1000
            gid: 1000
          - url: git+https://github.com/opensearch-project/query-insights-dashboards.git#3.7.0.0
            checksum: 1d4d0f8165fe5c872ebd3fb9472ec15e5742bfb6
            path: ${source.dir}/opensearch-dashboards/plugins/queryInsightsDashboards
            uid: 1000
            gid: 1000
      pipeline:
        - name: patch opensearch-dashboards with unified yarn patches
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards
        - name: patch opensearch-dashboards (including osd-ui-shared-deps)
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/opensearch-dashboards-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards
        - name: patch observabilityDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/dashboards-observability-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/observabilityDashboards
        - name: patch reportsDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/dashboards-reporting-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/reportsDashboards
        - name: patch queryWorkbenchDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/queryWorkbenchDashboards
        - name: patch anomalyDetectionDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/anomalyDetectionDashboards
        - name: patch indexManagementDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/indexManagementDashboards
        - name: patch notificationsDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/notificationsDashboards
        - name: patch alertingDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/alertingDashboards
        - name: patch securityAnalyticsDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/securityAnalyticsDashboards
        - name: patch securityDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/security-dashboards-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/securityDashboards
        - name: patch searchRelevanceDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/searchRelevanceDashboards
        - name: patch assistantDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/dashboards-assistant-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/assistantDashboards
        - name: patch customImportMapDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/dashboards-custom-import-map-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/customImportMapDashboards
        - name: patch mlCommonsDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/mlCommonsDashboards
        - name: patch flowFrameworkDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/no-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/flowFrameworkDashboards
        - name: patch queryInsightsDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/query-insights-dashboards-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/queryInsightsDashboards
        - name: patch investigationDashboards
          uses: yarn/bump@v2
          run-as: opensearch-dashboards
          privileged: true
          with:
            bumps: ${source.dir}/patches/dashboards-investigation-bumps.json
            yarn-root: ${source.dir}/opensearch-dashboards/plugins/investigationDashboards
        - name: Bootstrap opensearch-dashboards with plugins
          run-as: opensearch-dashboards
          work-dir: ${source.dir}/opensearch-dashboards
          privileged: true
          runs: |
            set -eux -o pipefail

            # yarn/bump@v2 installs with --ignore-scripts which breaks some dependencies.
            # Clean before bootstrapping/building so that everything is build properly.
            yarn osd clean
            yarn osd bootstrap --single-version 'ignore'
        - name: build all plugins
          run-as: opensearch-dashboards
          work-dir: ${source.dir}/opensearch-dashboards/plugins
          privileged: true
          runs: |
            set -eux -o pipefail

            plugin_dirs=$(ls -1d */ | sed 's:/$::')
            for dir_name in $plugin_dirs; do
              echo "Building plugin: ${dir_name}"
              cd "${dir_name}"
              yarn build
              cd ..
            done
        - name: patch source
          uses: git/patch@v1
          work-dir: ${source.dir}/opensearch-dashboards
          with:
            patch: ${source.dir}/patches/no_node_download.patch
        - name: build opensearch-dashboards
          run-as: opensearch-dashboards
          work-dir: ${source.dir}/opensearch-dashboards
          runs: |
            set -eux -o pipefail

            yarn run build-platform --${OPENSEARCH_BUILD_ARCH} --skip-os-packages --release
        - name: extract opensearch-dashboards
          work-dir: ${source.dir}/opensearch-dashboards
          runs: |
            set -eux -o pipefail

            # take the tarball in the build directory and extract it to the target directory
            ls -la ${source.dir}/opensearch-dashboards/build
            ls -la ${source.dir}/opensearch-dashboards/build/opensearch-dashboards
            ls -la ${source.dir}/opensearch-dashboards/build/opensearch-dashboards-3.7.0-${OPENSEARCH_DIST_ARCH}
            cp -r ${source.dir}/opensearch-dashboards/build/opensearch-dashboards ${target.dir}/opensearch-dashboards
        - name: extract plugin builds to target
          work-dir: ${source.dir}/opensearch-dashboards/plugins
          runs: |
            set -eux -o pipefail

            # Create plugins directory in target
            mkdir -p ${target.dir}/opensearch-dashboards/plugins

            # For each plugin directory, find a zip containing 3.7.0 (handles optional ".0") and extract it
            plugin_dirs=$(ls -1d */ | sed 's:/$::')
            for dir_name in $plugin_dirs; do
              build_dir="${dir_name}/build"
              echo "Processing plugin: ${dir_name}"
              if [ ! -d "${build_dir}" ]; then
                echo "Error: build directory not found for ${dir_name}, skipping"
                exit 1
              fi

              # Use find to locate a zip file that contains the VERSION in its name.
              # This handles names like plugin-3.7.0.zip and plugin-3.7.0.0.zip
              found_zip=$(find "${build_dir}" -maxdepth 1 -type f -iname "*3.7.0*.zip" | head -n 1 || true)

              if [ -z "${found_zip}" ]; then
                echo "Error: No zip found for plugin ${dir_name} containing version 3.7.0"
                exit 1
              fi

              echo "Found artifact: ${found_zip}, extracting to ${target.dir}/opensearch-dashboards/plugins/"
              unzip -q "${found_zip}" -d "${target.dir}/opensearch-dashboards/plugins/"
            done
        - name: copy over plugins to final dir inside opensearch-dashboards plugins dir
          runs: |
            set -eux -o pipefail

            mkdir -p ${source.dir}/tmp/plugins
            cp -r ${target.dir}/opensearch-dashboards/plugins/opensearch-dashboards/* ${source.dir}/tmp/plugins
            rm -rf ${target.dir}/opensearch-dashboards/plugins
            rm -rf ${source.dir}/tmp/plugins/opensearch-dashboards
            cp -r ${source.dir}/tmp/plugins ${target.dir}/opensearch-dashboards/plugins
      paths:
        - type: file
          path: /src/patches/dashboards-assistant-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/dashboards-assistant-bumps.json
        - type: file
          path: /src/patches/dashboards-custom-import-map-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/dashboards-custom-import-map-bumps.json
        - type: file
          path: /src/patches/dashboards-investigation-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/dashboards-investigation-bumps.json
        - type: file
          path: /src/patches/dashboards-observability-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/dashboards-observability-bumps.json
        - type: file
          path: /src/patches/dashboards-reporting-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/dashboards-reporting-bumps.json
        - type: file
          path: /src/patches/opensearch-dashboards-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/opensearch-dashboards-bumps.json
        - type: file
          path: /src/patches/query-insights-dashboards-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/query-insights-dashboards-bumps.json
        - type: file
          path: /src/patches/security-dashboards-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/security-dashboards-bumps.json
        - type: file
          path: /src/patches/no-bumps.json
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/no-bumps.json
        - type: file
          path: /src/patches/no_node_download.patch
          uid: 1000
          gid: 1000
          mode: "0600"
          source: image/opensearch-dashboards/patch/no_node_download.patch
      accounts:
        run-as: opensearch-dashboards
        users:
          - name: opensearch-dashboards
            uid: 1000
            gid: 1000
        groups:
          - name: opensearch-dashboards
            gid: 1000
            members:
              - opensearch-dashboards
      outputs:
        - source: ${target.dir}/opensearch-dashboards
          target: /usr/share/opensearch-dashboards
          uid: 0
          gid: 0
        - source: ${target.dir}/opensearch-dashboards/config
          target: /usr/share/opensearch-dashboards/config
          uid: 1000
          gid: 1000
        - source: /opt/docker/sbom/opensearch-dashboards
          target: /opt/docker/sbom/opensearch-dashboards
          uid: 0
          gid: 0
    - name: opensearch-dashboards files from upstream
      contents:
        packages:
          - bash
          - coreutils
          - findutils
        files:
          - url: git+https://github.com/opensearch-project/opensearch-build.git#3.6.0
            checksum: 18761b2bc0e7c224ff402da1380a5c6e56330f0f
            path: ${source.dir}/opensearch-build-18761b2bc0e7c224ff402da1380a5c6e56330f0f
            uid: 0
            gid: 0
      pipeline:
        - name: copy config and entrypoint files
          runs: |
            set -eux -o pipefail

            cp "${source.dir}/opensearch-build-18761b2bc0e7c224ff402da1380a5c6e56330f0f/docker/release/config/opensearch-dashboards/opensearch-dashboards-docker-entrypoint-3.x.sh" "${target.dir}/docker-entrypoint.sh"
            # /usr/share/opensearch-dashboards/config/opensearch_dashboards.yml
            cp "${source.dir}/opensearch-build-18761b2bc0e7c224ff402da1380a5c6e56330f0f/config/opensearch_dashboards-3.x.yml" "${target.dir}/opensearch_dashboards.yml"
        - name: customize config
          runs: |
            set -eux -o pipefail

            # This is what upstream does in their docker images so it is important to do this here
            echo "Setting default network host in opensearch-dashboards config..."
            echo "server.host: '0.0.0.0'" >> "${target.dir}/opensearch_dashboards.yml"
      outputs:
        - source: ${target.dir}/docker-entrypoint.sh
          target: /usr/local/bin/docker-entrypoint.sh
          uid: 0
          gid: 0
        - source: ${target.dir}/opensearch_dashboards.yml
          target: /usr/share/opensearch-dashboards/config/opensearch_dashboards.yml
          uid: 1000
          gid: 1000
accounts:
  run-as: opensearch-dashboards
  users:
    - name: opensearch-dashboards
      uid: 1000
      gid: 1000
  groups:
    - name: opensearch-dashboards
      gid: 1000
      members:
        - opensearch-dashboards
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /usr/share/opensearch-dashboards
environment:
  HOME: /usr/share/opensearch-dashboards
  OPENSEARCH_DASHBOARDS_HOME: /usr/share/opensearch-dashboards
  OPENSEARCH_DASHBOARDS_PATH_CONF: /usr/share/opensearch-dashboards/config
  PATH: /usr/share/opensearch-dashboards/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: directory
    path: /usr/share/opensearch-dashboards/data
    uid: 1000
    gid: 1000
  - type: directory
    path: /usr/share/opensearch-dashboards/logs
    uid: 1000
    gid: 1000
  - type: symlink
    path: /usr/share/opensearch-dashboards/node
    uid: 0
    gid: 0
    source: /usr
annotations:
  org.opencontainers.image.description: A minimal OpenSearch Dashboards image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/local/bin/docker-entrypoint.sh
cmd:
  - opensearch-dashboards
ports:
  - 5601/tcp
