diff --git a/pilot/pkg/model/authentication.go b/pilot/pkg/model/authentication.go
index 67bf22d..3420da6 100644
--- a/pilot/pkg/model/authentication.go
+++ b/pilot/pkg/model/authentication.go
@@ -15,7 +15,7 @@
 package model
 
 import (
-	"crypto/md5"
+	"crypto/sha256"
 	"fmt"
 	"strings"
 	"time"
@@ -167,9 +167,9 @@ func (policy *AuthenticationPolicies) addPeerAuthentication(configs []config.Con
 		policy.peerAuthentications[config.Namespace] = append(policy.peerAuthentications[config.Namespace], config)
 	}
 
-	// nolint: gosec
-	// Not security sensitive code
-	policy.aggregateVersion = fmt.Sprintf("%x", md5.Sum([]byte(strings.Join(versions, ";"))))
+	// Use SHA-256 instead of MD5 for FIPS compliance in aggregate version generation
+	sha256Hash := sha256.Sum256([]byte(strings.Join(versions, ";")))
+	policy.aggregateVersion = fmt.Sprintf("%x", sha256Hash)
 
 	// Process found namespace-level policy.
 	policy.namespaceMutualTLSMode = make(map[string]MutualTLSMode, len(foundNamespaceMTLS))
diff --git a/pkg/test/framework/components/istio/configmap.go b/pkg/test/framework/components/istio/configmap.go
index 78ef189..54931f5 100644
--- a/pkg/test/framework/components/istio/configmap.go
+++ b/pkg/test/framework/components/istio/configmap.go
@@ -16,7 +16,7 @@ package istio
 
 import (
 	"context"
-	"crypto/md5"
+	"crypto/sha256"
 	"encoding/hex"
 	"fmt"
 	"io"
@@ -446,9 +446,10 @@ func (cm *configMap) updateConfigMap(c cluster.Cluster, cfgMap *corev1.ConfigMap
 }
 
 func hash(s string) string {
-	// nolint: gosec
-	// Test only code
-	h := md5.New()
+	// Use SHA-256 instead of MD5 for FIPS compliance
+	// Note: This changes hash output format but is acceptable for test-only code
+	// where the hash is used for change detection, not cryptographic security
+	h := sha256.New()
 	_, _ = io.WriteString(h, s)
 	return hex.EncodeToString(h.Sum(nil))
 }
