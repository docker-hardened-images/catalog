# syntax=dhi.io/build:2-debian13

name: AWS CLI 1.x
image: dhi.io/awscli
variant: runtime
tags:
  - "1"
  - "1.45"
  - 1.45.8
  - 1-debian13
  - 1.45-debian13
  - 1.45.8-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  COMMIT_SHA: c3e005580e8f356b3ee20cd7b5c1ea6c18da27c3
  PYTHON_BUILD_REFERENCE: dhi/python:3.13.13-debian13-dev@sha256:19e0e6b0a08de7e47295401cb9273d2020f724bb7de6b67ba16576a44b2fa3b0
  PYTHON_REFERENCE: dhi/python:3.13.13-debian13@sha256:ce35145dcd0016ab6f17a1eade4e57d37850d05a1cfcee6190762a496567b440
  SEMVER_MAJOR_MINOR_VERSION: "1.45"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.45.8
  VERSION: 1.45.8
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - groff-base
    - less
    - libffi8
    - ncurses-base
    - ncurses-bin
    - zlib1g
  builds:
    - name: awscli
      uses: dhi.io/python:3.13.13-debian13-dev@sha256:19e0e6b0a08de7e47295401cb9273d2020f724bb7de6b67ba16576a44b2fa3b0
      contents:
        packages:
          - ca-certificates
          - coreutils
          - unzip
          - '!libexpat1'
        files:
          - url: https://s3.amazonaws.com/aws-cli/awscli-bundle-1.45.8.zip
            path: ${source.dir}/awscli.zip
            spdx:
              name: awscli
              version: 1.45.8
              packages:
                - name: awscli
                  purl: pkg:dhi/awscli@1.45.8
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: install
          work-dir: ${source.dir}
          runs: |
            set -eux -o pipefail

            unzip -q awscli.zip
            ./awscli-bundle/install -i "${target.dir}/opt/aws-cli"
        - name: bump
          work-dir: ${target.dir}/opt/aws-cli
          privileged: true
          runs: |
            set -eux -o pipefail

            ${target.dir}/opt/aws-cli/bin/python -m pip install --upgrade 'pyasn1>=0.6.2' # CVE-2026-23490
        - name: cleanup dependencies
          work-dir: ${target.dir}/opt/aws-cli
          runs: |
            set -eux -o pipefail

            ${target.dir}/opt/aws-cli/bin/python -m pip uninstall -y setuptools setuptools-scm wheel
        - name: fix paths
          runs: |
            set -eux -o pipefail

            # replace invalid reference to /out directory
            sed -i "s|${target.dir}/opt/aws-cli/bin/python|/opt/aws-cli/bin/python|g" ${target.dir}/opt/aws-cli/bin/aws
      outputs:
        - source: ${target.dir}/opt/aws-cli
          target: /opt/aws-cli
          uid: 0
          gid: 0
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/python:3.13.13-debian13@sha256:ce35145dcd0016ab6f17a1eade4e57d37850d05a1cfcee6190762a496567b440
      includes:
        - opt/**
        - usr/**
      uid: 0
      gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  AWS_CLI_VERSION: 1.45.8
  PATH: /opt/aws-cli/bin:/usr/sbin:/sbin:/usr/bin:/bin
annotations:
  org.opencontainers.image.description: A minimal AWS CLI image
cmd:
  - aws
  - help
