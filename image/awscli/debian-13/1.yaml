# syntax=dhi.io/build:2-debian13

name: AWS CLI 1.x
image: dhi.io/awscli
variant: runtime
tags:
  - "1"
  - "1.44"
  - 1.44.63
  - 1-debian13
  - 1.44-debian13
  - 1.44.63-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  COMMIT_SHA: dbdac85253d5a7e859807db1c4f12724033f8b31
  PYTHON_BUILD_REFERENCE: dhi/python:3.13.12-debian13-dev@sha256:bf462648cb31fe9b36464e637c705bc3d0643ce9abfd22d45adbe07c61514a3b
  PYTHON_REFERENCE: dhi/python:3.13.12-debian13@sha256:209a1d7ff557c90f18d063ee6ad0a915adc5ec85105c84ca094d945b87b9933c
  SEMVER_MAJOR_MINOR_VERSION: "1.44"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.44.63
  VERSION: 1.44.63
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - groff-base
    - less
    - libffi8
    - ncurses-base
    - ncurses-bin
    - zlib1g
  builds:
    - name: awscli
      uses: dhi.io/python:3.13.12-debian13-dev@sha256:bf462648cb31fe9b36464e637c705bc3d0643ce9abfd22d45adbe07c61514a3b
      contents:
        packages:
          - ca-certificates
          - coreutils
          - unzip
          - '!libexpat1'
        files:
          - url: https://s3.amazonaws.com/aws-cli/awscli-bundle-1.44.63.zip
            path: ${source.dir}/awscli.zip
            spdx:
              name: awscli
              version: 1.44.63
              packages:
                - name: awscli
                  purl: pkg:dhi/awscli@1.44.63
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: install
          work-dir: ${source.dir}
          runs: |
            set -eux -o pipefail

            unzip -q awscli.zip
            ./awscli-bundle/install -i "${target.dir}/opt/aws-cli"
        - name: bump
          work-dir: ${target.dir}/opt/aws-cli
          privileged: true
          runs: |
            set -eux -o pipefail

            ${target.dir}/opt/aws-cli/bin/python -m pip install --upgrade 'pyasn1>=0.6.2' # CVE-2026-23490
        - name: cleanup dependencies
          work-dir: ${target.dir}/opt/aws-cli
          runs: |
            set -eux -o pipefail

            ${target.dir}/opt/aws-cli/bin/python -m pip uninstall -y setuptools setuptools-scm wheel
        - name: fix paths
          runs: |
            set -eux -o pipefail

            # replace invalid reference to /out directory
            sed -i "s|${target.dir}/opt/aws-cli/bin/python|/opt/aws-cli/bin/python|g" ${target.dir}/opt/aws-cli/bin/aws
      outputs:
        - source: ${target.dir}/opt/aws-cli
          target: /opt/aws-cli
          uid: 0
          gid: 0
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/python:3.13.12-debian13@sha256:209a1d7ff557c90f18d063ee6ad0a915adc5ec85105c84ca094d945b87b9933c
      includes:
        - opt/**
        - usr/**
      uid: 0
      gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  AWS_CLI_VERSION: 1.44.63
  PATH: /opt/aws-cli/bin:/usr/sbin:/sbin:/usr/bin:/bin
annotations:
  org.opencontainers.image.description: A minimal AWS CLI image
cmd:
  - aws
  - help
