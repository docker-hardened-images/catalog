# syntax=dhi.io/build:2-debian13

name: Envoy 1.37.x (dev)
image: dhi.io/envoy
variant: dev
tags:
  - 1.37-dev
  - 1.37.3-dev
  - 1.37-debian-dev
  - 1.37-debian13-dev
  - 1.37.3-debian-dev
  - 1.37.3-debian13-dev
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-01-13"
  end-of-life: "2027-01-13"
vars:
  BASE_REFERENCE: dhi/static:20250419-glibc-debian13@sha256:57cb778d81d0b8b2f9407c646813a6a09f33f82c540aed1060c6f3d83f9d9858
  ENVOY_ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch_64" }'
  SEMVER_MAJOR_MINOR_VERSION: "1.37"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.37.3
  VERSION: 1.37.3
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!base-files'
    - '!gawk'
    - '!libelogind0'
    - '!original-awk'
    - apt
    - bash
    - ca-certificates
    - coreutils
    - diffutils
    - dpkg
    - findutils
    - grep
    - libc-bin
    - mawk
    - perl-base
    - sed
    - util-linux
  builds:
    - name: envoy
      contents:
        packages:
          - coreutils
          - bash
          - grep
          - dirmngr
          - gpg
          - gpg-agent
        files:
          - url: https://github.com/envoyproxy/envoy/releases/download/v1.37.3/checksums.txt.asc
            path: ${source.dir}/checksums.txt.asc
            uid: 0
            gid: 0
          - url: https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x0AFCE836BA4D1D35763C8523D8CDC3750181F31F
            path: ${source.dir}/envoy-maintainers.gpg
            uid: 0
            gid: 0
          - url: https://github.com/envoyproxy/envoy/releases/download/v1.37.3/envoy-1.37.3-linux-${ENVOY_ARCH}
            path: ${source.dir}/envoy-1.37.3-linux-${ENVOY_ARCH}
            spdx:
              name: envoy
              version: 1.37.3
              packages:
                - name: envoy
                  purl: pkg:dhi/envoy@1.37.3
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: verify
          runs: |
            set -eux -o pipefail

            # GPG verification
            gpg --batch --yes --import envoy-maintainers.gpg
            gpg --batch --yes --output checksums.txt --decrypt checksums.txt.asc

            # Extract and rewrite checksum line
            awk "/envoy-1.37.3-linux-${ENVOY_ARCH}\$/ {
              printf \"%s  envoy-%s-linux-%s\\n\", \$1, \"1.37.3\", \"${ENVOY_ARCH}\"
            }" checksums.txt > checksum-single.txt

            [ -s checksum-single.txt ] || {
              echo "Error: Checksum for envoy-1.37.3-linux-${ENVOY_ARCH} not found in checksums.txt.asc"
              exit 1
            }

            # Validate directly in ${source.dir}
            sha256sum -c checksum-single.txt
        - name: install
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/usr/local/bin"
            cp "${source.dir}/envoy-1.37.3-linux-${ENVOY_ARCH}" "${target.dir}/usr/local/bin/envoy"
      outputs:
        - source: ${target.dir}/usr/local/bin/envoy
          target: /usr/local/bin/envoy
          uid: 0
          gid: 0
          mode: "0755"
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/static:20250419-glibc-debian13@sha256:57cb778d81d0b8b2f9407c646813a6a09f33f82c540aed1060c6f3d83f9d9858
      includes:
        - bin
        - etc
        - home
        - lib
        - lib64
        - opt
        - sbin
        - tmp
        - usr
        - var
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  DEBIAN_FRONTEND: noninteractive
annotations:
  org.opencontainers.image.description: A minimal Envoy image
  org.opencontainers.image.licenses: Apache-2.0
cmd:
  - envoy
